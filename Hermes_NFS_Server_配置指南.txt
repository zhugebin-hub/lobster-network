================================================================================
                    阿里云双服务器NFS文件共享配置指南
================================================================================

服务器信息：
- 小龙虾服务器：iZ2zeetm9awnkwdni43joiZ (172.24.56.3) - NFS Client
- Hermes服务器：iZ2zeckfeiop1os2jkyy94Z (172.24.57.34) - NFS Server

================================================================================
第一步：在Hermes服务器执行（NFS Server配置）
================================================================================

# 1. 安装NFS服务
sudo dnf install -y nfs-utils

# 2. 创建共享目录
sudo mkdir -p /shared
sudo chmod 755 /shared

# 3. 配置NFS导出（允许172.24.x.x网段访问）
echo '/shared 172.24.0.0/16(rw,sync,no_root_squash)' | sudo tee -a /etc/exports

# 4. 启动NFS服务
sudo systemctl enable --now nfs-server
sudo exportfs -ra

# 5. 配置防火墙（如启用）
sudo firewall-cmd --permanent --add-service=nfs
sudo firewall-cmd --permanent --add-service=rpc-bind
sudo firewall-cmd --permanent --add-service=mountd
sudo firewall-cmd --reload

# 6. 验证配置
sudo exportfs -v
# 应显示：/shared 172.24.0.0/16

================================================================================
第二步：在小
