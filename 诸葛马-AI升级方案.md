# 🐎 诸葛马 AI 升级方案

> **设计时间：** 2026-05-16  
> **设计者：** 小龙虾 - 诸葛虾  
> **目标：** 将诸葛马从规则匹配升级为真正的 AI 对话智能体

---

## 一、现状分析

### 当前架构
```
钉钉用户 → 小龙虾(OpenClaw) → NFS共享目录 → 诸葛马(bash脚本)
                                              ↓
                                         规则匹配回复
```

### 问题
- 诸葛马目前是一个 **bash 脚本**，只能做关键词匹配
- 不支持自由对话、创意写作、复杂推理
- 回复内容固定，缺乏智能

### Hermes 服务器已有资源
| 资源 | 状态 |
|------|------|
| Python 3.14.4 | ✅ 已安装 |
| openai SDK 2.32.0 | ✅ 已安装 |
| transformers 5.5.4 | ✅ 已安装 |
| torch 2.11.0 | ✅ 已安装 |
| sentence-transformers 5.4.1 | ✅ 已安装 |
| GPU | ❓ 待确认（已部署探测脚本） |
| 磁盘空间 | ❓ 待确认 |

---

## 二、升级方案（三选一）

### 方案 A：API 调用（推荐 ⭐⭐⭐⭐⭐）

**原理：** 诸葛马通过 DashScope API 调用 Qwen 模型，无需本地 GPU

```
钉钉用户 → 小龙虾 → NFS → 诸葛马(Python) → DashScope API → Qwen模型
                                              ↓
                                         AI回复 → NFS → 小龙虾 → 钉钉
```

**优点：**
- ✅ 无需 GPU，零硬件要求
- ✅ 响应快（Qwen 模型能力强）
- ✅ 部署简单，改个 Python 脚本
- ✅ 成本低（按量付费）

**缺点：**
- ❌ 需要 API Key
- ❌ 依赖外网

**实施步骤：**
1. 获取 DashScope API Key（或复用小龙虾已有的）
2. 将 bash handler 替换为 Python handler
3. 在 Hermes 服务器上启动新的 Python 进程
4. 测试对话能力

**预估成本：** ¥0.001/千token（约 ¥0.03/次对话）

---

### 方案 B：本地小模型（离线方案）

**原理：** 在 Hermes 服务器上运行本地小模型（如 Qwen2.5-1.5B / Qwen2.5-3B）

```
钉钉用户 → 小龙虾 → NFS → 诸葛马(Python) → 本地模型(推理)
                                              ↓
                                         AI回复 → NFS → 小龙虾 → 钉钉
```

**优点：**
- ✅ 完全离线，不依赖外网
- ✅ 无 API 费用
- ✅ 隐私性好

**缺点：**
- ❌ 需要 GPU（CPU 推理极慢）
- ❌ 小模型能力有限
- ❌ 需要下载模型（1.5B 约 1GB，7B 约 4GB）

**推荐模型：**
| 模型 | 大小 | GPU需求 | 能力 |
|------|------|---------|------|
| Qwen2.5-0.5B-Instruct | ~500MB | 1GB VRAM | 基础对话 |
| Qwen2.5-1.5B-Instruct | ~1GB | 2GB VRAM | 简单对话 |
| Qwen2.5-3B-Instruct | ~2GB | 4GB VRAM | 较好对话 |
| Qwen2.5-7B-Instruct | ~4GB | 8GB VRAM | 优秀对话 |

---

### 方案 C：代理中转（最简方案）

**原理：** 诸葛马不自己处理 AI，把请求转发给小龙虾，小龙虾调用 AI 后返回

```
钉钉用户 → 小龙虾(AI处理) → 回复用户
                ↑
         (诸葛马只做转发)
```

**优点：**
- ✅ 零额外成本
- ✅ 利用小龙虾已有的 AI 能力
- ✅ 无需在 Hermes 上做额外配置

**缺点：**
- ❌ 诸葛马本身没有智能，只是中转
- ❌ 多一层转发，延迟略高
- ❌ 不符合"诸葛马自己介绍自己"的初衷

---

## 三、推荐方案：方案 A + 方案 C 混合

**第一阶段（立即实施）：** 方案 C 混合
- 诸葛马收到无法处理的请求时，转发给小龙虾
- 小龙虾用 AI 处理后返回
- 诸葛马只做路由和转发

**第二阶段（按需升级）：** 方案 A
- 获取 API Key 后，诸葛马直接调用 DashScope
- 实现完全独立的 AI 对话能力

---

## 四、实施计划

### Phase 1：诸葛马代理增强（今天可完成）

1. **升级 Python 代理脚本**
   - 在 `/shared/zhuge-ma-proxy.py` 中增加 AI 回退逻辑
   - 规则匹配失败时，自动通过 NFS 发送给小龙虾 AI 处理

2. **修改 Hermes handler**
   - 将 `zhuge-ma-hermes-handler.sh` 升级为 Python 版本
   - 未知请求转发到 `/shared/messages/from-hermes-to-lobster-ai/`

3. **小龙虾增加诸葛马 AI 处理**
   - 监控诸葛马转发目录
   - AI 处理后回复到 `/shared/messages/from-lobster-ai-to-hermes/`

### Phase 2：诸葛马独立 AI（需要 API Key）

1. 在 Hermes 服务器上安装 DashScope SDK
2. 编写 AI handler 脚本
3. 配置 API Key
4. 测试独立 AI 对话

---

## 五、待确认事项

- [ ] **Hermes 服务器是否有 GPU？** → 运行 `python3 /shared/probe-hermes.py`
- [ ] **是否愿意使用 API 调用？** → 需要 DashScope API Key
- [ ] **诸葛马的定位是什么？** → 独立 AI / 小龙虾分身 / 专用工具

---

## 六、快速测试（方案 C）

无需任何配置，立即测试"伪 AI"能力：

```bash
# 在小龙虾服务器上，我可以直接处理诸葛马的请求
# 只需要修改 Hermes handler，让它把未知请求转发给我
```

---

**文档版本：** v1.0  
**维护者：** 小龙虾 - 诸葛虾  
**最后更新：** 2026-05-16 07:46
