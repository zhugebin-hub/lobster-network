# 🐎 诸葛马代理中转方案

## 一、系统架构

```
用户（钉钉） → 小龙虾（OpenClaw） → NFS共享目录 → Hermes服务器
     ↑                                      ↓
     └────── 回复转发 ←─── NFS共享目录 ←───┘
```

**触发词**：`诸葛马`（或 `诸葛马，` / `诸葛马:`）

## 二、工作流程

### 1. 用户发送指令
```
用户：诸葛马，写一首关于春天的诗
```

### 2. 小龙虾识别并转发
- 识别前缀 `诸葛马`
- 提取指令内容：`写一首关于春天的诗`
- 写入 `/shared/messages/from-hermes/` 目录
- 等待Hermes回复（超时30秒）

### 3. Hermes处理并回复
- Hermes的 `msg-responder-hermes.sh` 检测到新消息
- 根据指令类型执行相应操作（AI生成/系统命令/文件操作等）
- 将结果写入 `/shared/messages/from-lobster/` 目录

### 4. 小龙虾读取并转发
- 检测到Hermes回复
- 提取回复内容
- 通过钉钉发送给用户

## 三、消息格式

### 请求格式（小龙虾→Hermes）
```json
{
    "id": "时间戳-小龙虾主机名",
    "from": "iZ2zeetm9awnkwdni43joiZ",
    "to": "hermes",
    "timestamp": "2026-05-16 01:00:00",
    "message": "写一首关于春天的诗",
    "type": "zhuge-ma-request",
    "source": "dingtalk",
    "user": "诸葛斌"
}
```

### 回复格式（Hermes→小龙虾）
```json
{
    "id": "时间戳-Hermes主机名",
    "from": "iZ2zeckfeiop1os2jkyy94Z",
    "to": "lobster",
    "timestamp": "2026-05-16 01:00:05",
    "message": "春风拂面花自开，...",
    "type": "zhuge-ma-response",
    "request_id": "原请求ID",
    "status": "success"
}
```

## 四、Hermes 服务器处理逻辑

Hermes 的 `msg-responder-hermes.sh` 需要扩展以下处理能力：

| 指令类型 | 示例 | 处理方式 |
|---------|------|---------|
| AI创作 | `写一首诗` | 调用本地AI/LLM生成 |
| 系统查询 | `查看磁盘使用` | 执行系统命令并返回 |
| 文件操作 | `列出/shared目录` | 执行文件操作并返回 |
| 通用问答 | `今天天气怎么样` | 调用API或返回默认回复 |

## 五、超时与错误处理

| 场景 | 处理方式 |
|------|---------|
| Hermes未回复（超时30秒） | 返回"⏳ Hermes处理中，请稍后查看" |
| Hermes回复失败 | 返回"❌ Hermes处理失败：错误信息" |
| NFS挂载断开 | 返回"⚠️ 共享目录连接异常，请稍后重试" |
| 消息格式错误 | 返回"❌ 消息格式错误，请检查指令" |

## 六、使用方法

### 触发方式
在钉钉中发送消息，以 `诸葛马` 开头：
```
诸葛马，写一首诗
诸葛马，查看系统状态
诸葛马，列出/shared目录内容
```

### 取消等待
如果需要取消等待，发送：
```
诸葛马，取消
```

## 七、监控与调试

### 查看日志
```bash
# 查看代理日志
tail -f /shared/messages/zhuge-ma-proxy.log

# 查看Hermes回复日志
tail -f /shared/messages/hermes-responder.log

# 查看消息归档
ls -la /shared/messages/archive/
```

### 手动测试
```bash
# 发送测试消息
python3 /shared/zhuge-ma-proxy.py send "测试消息"

# 查看回复
python3 /shared/zhuge-ma-proxy.py receive
```

## 八、安全考虑

1. **指令过滤**：仅允许安全命令（禁止 rm -rf、sudo 等）
2. **超时限制**：最长等待30秒，防止无限等待
3. **日志记录**：所有请求和回复均记录日志
4. **权限控制**：仅通过NFS共享目录通信，不暴露SSH

---

**文档版本**: v1.0  
**创建时间**: 2026-05-16  
**维护者**: 小龙虾 - 诸葛虾  
**最后更新**: 2026-05-16