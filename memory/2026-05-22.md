# 2026-05-22 (周五)

## 🎓 毕业论文系统修改（thesis-instance-1 & thesis-instance-2）

### 1. 论文评审模块放开限制（第一导师）
- **需求**：导师论文评审模块只能看到分流学生，需要放开到所有学生
- **问题定位**：`db.ts` 第 2657 行 `eq(users.studentType, "transfer")` 过滤了非分流学生
- **修改**：删除第 2657 行过滤条件
- **备份**：`server/db.ts.bak.20260522`
- **回退命令**：`cp server/db.ts.bak.20260522 server/db.ts && npm run build && pm2 restart thesis-instance-1`
- **注意**：需要 `npm run build` 后重启才生效（PM2 运行的是编译后的 `dist/index.js`）

### 2. 题目修改模块放开限制（非分流学生）
- **需求**：题目修改模块只对分流学生可见，需要对所有学生开放
- **问题定位**：`StudentDashboard.tsx` 第 518 行 `{isTransfer && match && (` 控制了入口显示
- **修改**：改为 `{match && (`
- **备份**：`client/src/pages/StudentDashboard.tsx.bak.20260522`
- **回退命令**：`cp client/src/pages/StudentDashboard.tsx.bak.20260522 client/src/pages/StudentDashboard.tsx && npm run build && pm2 restart thesis-instance-1`
- **注意**：需要 `Ctrl + Shift + R` 强制刷新浏览器清除缓存

### 3. thesis-instance-2 同步修改
- 同样的两处修改：
  - `db.ts` 第 2671 行删除 `studentType == "transfer"` 过滤
  - `StudentDashboard.tsx` 第 501 行去除 `isTransfer` 限制
- 备份：`server/db.ts.bak.20260522` + `client/src/pages/StudentDashboard.tsx.bak.20260522`

### 4. 代码导出备份
- thesis-instance-1 导出到本地：`thesis-instance-1-export.tar.gz`（8.3M）
- thesis-instance-2 导出到本地：`thesis-instance-2-export.tar.gz`（8.3M）
- 服务器 IP：10.20.33.63
- 导出命令：`tar czf thesis-instance-X-export.tar.gz --exclude='node_modules' --exclude='*.bak.*' --exclude='backups' thesis-instance-X/`

### 5. 数据库备份接收
- thesis_system_1 备份：`thesis_system_1_20260522_074515.sql.gz`（208K）
- thesis_system_2 备份：`thesis_system_2_20260519_054050.sql.gz`（415K）

### 6. thesis_system_2 数据查询（廖长庚 & Romeo Glovnea）
- **Romeo Glovnea (teacherId=1326)**：6 个题目，5 个 published（未确认），1 个 used
  - 295: Design and build a rig for study of the force generated by land snails' movement — published
  - 296: Study of the use of a mini toroidal CVT in the end effector of a robot — published
  - 297: Study of stresses in a model robotic arm by photoelasticity — published
  - 298: Robotic vehicle with safe collision mechanical transmission — used（已确认）
  - 299: Electrical Conductivity and Friction Force Evaluation — published
  - 300: Design, build and test a rig for the evaluation of the gyroscopic effect — published
- **廖长庚 (teacherId=1328)**：2 个题目，均为 published（未确认）
  - 304: Quantum-classical hybrid neural network for image classification — published
  - 305: Optimizing Adiabatic Evolution-Based Quantum State Preparation via ML — published
- **有学生选择但未确认的题目**：多个 published 题目已有学生志愿申请（pending 状态），但导师尚未确认匹配

### 7. 定稿确认功能（待开发）
- **需求**：在论文评审模块增加"定稿确认"按钮
- **规则**：只有中方导师（第一导师）或英方导师的第二导师（中方）可点定稿
- **数据库**：thesisDrafts 表需新增 `finalConfirmed`、`finalConfirmedBy`、`finalConfirmedAt` 字段
- **用途**：批量导出论文用于省级抽检

## 🏠 智能家居智能体教学案例（诸葛斌老师）

**需求：** 诸葛斌老师要求整理智能家居场景的深度解析为教学案例文档和PPT大纲
**内容：**
- 场景一：下班回家的完整智能体协作链（五幕场景，12个智能体协作）
- 场景二：智能设备全生命周期管理（购买→配送→安装→使用→维修→二手交易→回收）
- 技术架构：LLM意图理解 + 多智能体协作 + 群聊协议 + 区块链存证
- 教学设计：课堂讨论题、项目实践建议、考核方式
**产出文件：**
- `教学案例_智能体数字员工与智能家居.docx`（26K）— 已发送诸葛斌
- `PPT大纲_智能体数字员工与智能家居.docx`（19K）— 已发送诸葛斌
- `智能体数字员工与智能家居.pptx`（49K）— 已发送诸葛斌（12张幻灯片，专业配色）
- `教学案例_智能体数字员工与智能家居_完整版.docx` — 根据完整聊天重新整理的完整版
- `PPT大纲_智能体数字员工与智能家居_完整版.docx` — 30张幻灯片完整版大纲
- `智能体数字员工与智能家居_完整版.pptx`（83K）— 30张幻灯片完整版PPT，已发送诸葛斌
- Markdown源文件同样保存在workspace根目录
- Python生成脚本：`generate_ppt.py` + `generate_ppt_full.py`

## 📌 关键经验
- TypeScript 项目修改后必须 `npm run build` 再重启 PM2（运行的是编译产物 `dist/index.js`）
- 前端修改后需 `Ctrl + Shift + R` 强制刷新清除浏览器缓存
- 修改前先备份，回退命令提前准备好
