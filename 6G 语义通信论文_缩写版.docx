面向 6G 语义通信的深度学习边缘智能体知识演化与自适应传输机制

诸葛斌 1，洪仕玉 1，许云汉 1，董黎刚 1，张子天 1，蒋献 1
（浙江工商大学信息与电子工程学院（萨塞克斯人工智能学院），浙江 杭州 310018）

摘  要：面向 6G 语义通信中分布式协同面临的高昂信令开销与环境非平稳性挑战，本文提出一种面向深层语义智能体的知识演化与自适应传输机制。首先，将边缘协同过程严密建模为以感知节点为主体的部分可观察马尔可夫决策过程（POMDP），并引入局部敏感哈希（LSH）算子将高维语义特征投影至低维流形，以构建轻量级的动态知识对齐字典。其次，提出知识增强型近端策略优化（KE-PPO）算法，利用信息瓶颈（IB）理论重构联合奖励函数，并引入基于策略熵驱动的动态融合权重机制。基于真实视觉数据集与时变衰落信道的实验表明，智能体仅凭局部观测即可自主输出环境适配的多级语义传输策略，显著降低了传统分布式方案中繁重的全局交互负担，有效缓解了环境非平稳带来的决策瓶颈。实验结果证实，该机制显著改善了传统纯强化学习在非平稳环境中的策略震荡问题，在极端信道下展现出优异的鲁棒性，有效逼近了推理精度与通信时延之间的帕累托最优前沿。

关键词：6G 语义通信；自演化知识库；近端策略优化（PPO）；自适应协同传输

1  引言

随着移动通信技术从 5G 向 6G 演进，未来通信网络正经历着从单纯追求比特可靠传输向注重语义有效传递与理解的深刻变革。6G 愿景下的万物智联、全息通信和数字孪生等新兴应用场景，不仅催生了海量的多模态数据，更对通信系统的智能化、低时延与高能效提出了前所未有的严苛要求。在频谱资源日益枯竭和边缘设备能量受限的背景下，实现以用户意图为中心的高效通信已成为必然趋势。然而，基于香农信息论建立的传统通信系统长期致力于物理层和链路层优化，这种"比特忠实度"的范式在面对 6G 时代的海量数据暴增时，已逐渐显露出日益凸显的带宽瓶颈，难以满足未来网络对超可靠低时延（URLLC）的极致追求。

语义通信（Semantic Communication）作为一种突破香农范式瓶颈的新兴架构，旨在仅提取并传输与下游任务高度相关的核心语义信息，从而大幅滤除冗余数据，并在极低信噪比（SNR）等恶劣信道环境下表现出卓越的鲁棒性。现有语义通信的研究多局限于单点传输或静态编解码优化，并通常建立在一个较为理想的假设之上：即假定收发双方始终具备全局共享且静态同构的"先验知识库"。然而，在实际的 6G 分布式边缘计算网络中，边缘设备因物理位置、观测视角与算力的差异，智能体之间的知识状态存在客观的异构性。当环境发生突变时，这种"知识失配"将导致严重的语义歧义与通信效能下降。

在复杂的边缘协同推理场景中，为应对动态时变信道与资源分配挑战，近年来部分前沿研究尝试引入多智能体强化学习（MARL）进行联合通信决策。然而，在真实的 6G 物联网场景中，边缘节点往往面临严苛的电量与带宽约束，要求多个节点之间高频交互全局信道状态信息（CSI）与策略参数以维持 MARL 的收敛，将产生庞大的交互开销，难以满足 6G 的低时延要求。此外，多智能体在处理高维动态环境时容易面临"环境非平稳性（Non-stationarity）"与维度灾难等难题，导致算法震荡且难以在毫秒级实时部署。更为关键的是，现有研究未能量化并利用通信过程中"知识状态"的时序动态演化差异。

鉴于此，为缓解分布式协同方案中的高昂信令开销与非平稳性难题，本文提出一种面向深层语义智能体的知识演化与自适应传输机制。本文将通信交互过程严密建模为以边缘感知节点为主体的部分可观察马尔可夫决策过程（POMDP），利用基于哈希索引的动态经验字典表征语义知识库，并提出知识增强的近端策略优化（KE-PPO）算法进行协同策略求解。本文的主要贡献归纳为以下四个方面：

（1）提出了融合 LSH 语义流形的边缘协同通信框架。针对多智能体协同对全局信息依赖度高的问题，构建了单智能体 POMDP 模型。引入局部敏感哈希（LSH）映射机制确立了高维特征向低维流形投影的误差界限，在有效降低决策空间维度的同时，构建了轻量级的动态知识字典。

（2）设计了基于信息瓶颈（IB）理论的知识演化机制。引入动态演化范式，将通信传输代价与推理意图互信息之间的博弈重构为 IB 约束下的联合寻优问题。驱动经验字典随物理信道状态时序更新，促进了环境动态与核心意图沉淀的协同适配。

（3）提出了基于策略熵驱动的动态融合传输机制（KE-PPO）。构建了由策略熵量化决策不确定性的自适应融合机制，将深度强化学习的全局探索特征与演化知识库的先验经验进行数学耦合，有效缓解了高动态非平稳环境下静态超参数设定引发的动作选择崩溃风险。

（4）构建了结合真实多模态数据集与时变信道的评估矩阵。仿真实验与机理分析表明，所提机制在极低信令开销下，有效逼近了全局完美感知下的帕累托最优前沿；在极端拥塞的衰落信道中，系统通过主动降级展现出优异的鲁棒性，有效克服了传统纯强化学习的探索震荡问题。

2  系统模型与问题建模

2.1  分布式边缘协同语义通信系统模型

本文考虑一个面向 6G 智联网（AIoT）的分布式边缘协同通信系统。在该网络架构中，系统由多个配备有限算力与射频模块的无线边缘节点（如智能视觉传感器、无人机或边缘网关）组成。在传统的通信范式下，海量的原始感知数据直接通过无线信道传输，极易在带宽受限或深度衰落的环境下引发极高的传输时延与系统拥塞。

为了突破这一物理瓶颈，本文以典型的"分布式协同视觉推理任务"为例，构建了端边协同语义通信系统。在该模型中，系统节点被划分为两大核心实体：

发送智能体 A（边缘感知节点）：部署于数据生成的源头。其不仅负责在边缘端采集高维的原始视觉信号，更关键的是利用内置的语义算子执行"语义特征提取"。智能体 A 将剔除视觉冗余信息，抽取出低维的核心语义特征，并将其转化为适合无线传输的比特流。本文所提架构具有普适性，仿真中采用轻量级 ResNet-18 作为语义算子以适配边缘算力约束，但该机制可平滑迁移至参数量更大、表征能力更强的深层语义模型中。

接收智能体 B（边缘汇聚节点）：通常部署于算力充沛的边缘汇聚服务器（MEC）端。由于无线传输过程必然受到噪声、多径效应与信道衰落的干扰，接收智能体 B 将接收到受损的语义流。其内部包含语义解码模块与协同推理执行模块，负责对受损语义进行纠错、融合，并完成最终的下游意图推理（如目标检测、场景分类）。

2.2  语义知识库（SKB）与状态矩阵演化

在实际的边缘协同场景中，由于收发双方物理位置的差异、环境观测视角的局部性以及无线信道的时变性，发送端与接收端之间往往存在不可避免的语义理解偏差。为缓解这种"知识非同步"导致的推理失效，本文研究并为深层语义智能体引入了演化型语义知识库（Semantic Knowledge Base, SKB）。在数学表征上，为了适应底层强化学习的决策逻辑，我们将 SKB 显式定义为一个映射状态 - 动作经验价值的动态记忆字典 K(s, a)。

需要指出的是，本机制中的"自演化"与传统边缘缓存（Edge Caching）或经验回放（Experience Replay）存在本质区别。传统缓存通常依赖静态的键值匹配来存储原始特征或固定信道状态，难以应对 6G 高动态环境下的特征分布漂移（Distribution Shift）。而本文的 SKB 本质上是一个随环境非平稳性动态更新的概率演化流形。在每一轮通信决策后，知识库的状态更新方程定义为：

K(s_t, a_t)_{t+1} = K(s_t, a_t)_t + η·(R_t - K(s_t, a_t)_t)

此更新并非对单次交互的简单机械记录，而是在信息瓶颈约束下，对高维语义状态在低维子空间上的期望效用（Expected Utility）进行的后验概率近似。通过引入衰减因子η，SKB 能够有效平滑高频时变信道带来的瞬态噪声，使隐层特征分布与接收端的真实意图需求随时间演进实现动态对齐。

2.3  协同传输策略的边缘单智能体 POMDP 重构

在真实的分布式网络中，边缘感知节点受限于物理距离与信令开销，无法获取全局网络拓扑或其他接收节点的真实内在状态，其只能依靠本地侦测进行不完全感知。因此，本文摒弃了极易引发维度爆炸与非平稳性的多智能体联合建模，将该协同传输策略优化问题严密重构为以边缘感知节点为主体的单智能体部分可观察马尔可夫决策过程（Partially Observable Markov Decision Process, POMDP）。

该边缘决策 POMDP 在数学上由元组⟨S, A, P, R, Ω, O⟩刻画，具体定义如下：
- S：全局真实状态空间，包含整个网络的全局动态 CSI 与所有节点的真实内部状态矩阵。
- A：联合动作空间，即各个感知节点基于本地大模型（LLM）评估后，实时选择的语义特征降级与压缩级别。
- P: S × A → Δ(S)：状态转移概率，这不仅受外界物理层动态衰落信道变化的随机影响，同时还严格遵循内部知识库演化规律。
- R：联合奖励函数，本文基于信息瓶颈（IB）理论，将其严密定义为意图互信息最大化与通信压缩开销最小化的帕累托折中目标函数。
- Ω与 O：局部观察空间与观察函数。由于环境的部分可观测性，感知节点 i 在 t 时刻的局部观察 o_i(t) ∈ Ω不仅包含其侦测到的物理信道信噪比（SNR），更隐式融合了表征收发双方知识异构性的失配度（KL 散度代理特征）。通过 LSH 算子降维后的本地状态矩阵 K_i(t) 构成了核心的观测空间。

3  基于 KE-PPO 的边缘协同传输优化算法

3.1  基于知识库增强的 PPO 协同传输架构

在分布式边缘协同语义通信系统中，由于物理信道的时变衰落特性，多智能体联合优化通常面临极高的全局信道状态信息（CSI）交互开销。传统的边缘多智能体强化学习（Multi-Agent Reinforcement Learning, MARL）在处理此类问题时，极易陷入'环境非平稳性（Non-stationarity）'与维数灾难陷阱。为此，本文并未采用冗余的全局联合多智能体架构，而是将协同传输策略优化问题严密重构为以边缘感知节点为主体的独立单智能体部分可观察马尔可夫决策过程（POMDP），并提出知识增强型近端策略优化（KE-PPO）算法。该架构通过引入动态语义知识库（SKB），在规避高昂节点间信令开销的同时，有效弥补了单智能体局部观测的局限性。

3.2  联合状态特征提取与知识库降维映射

在强化学习中，状态空间的设计直接决定了神经网络的拟合效率与算法的最终性能。在本文的真实实验实现中，为了充分刻画"信源语义"与"信道状态"的耦合关系，边缘智能体 i 的局部观测向量 o_i(t) 被严密定义为一个 529 维的联合连续特征向量。具体而言：前 512 维为前端算力提取的高维语义特征；中间 16 维为当前物理信噪比（SNR）特征；最后 1 维为衡量收发双方知识异构性的 KL 散度（知识失配度）指标。

此外，每个智能体维护的动态语义知识库 K_i(t) 本质上是一个高维连续状态的映射字典。直接对 529 维的联合状态进行离散化建库将不可避免地引发维度灾难。考虑到 6G 视觉语义特征并非在整个欧氏空间中均匀分布，而是高度聚集在一个低维语义流形（Semantic Manifold）上，本文提出一种基于局部敏感哈希（Locality-Sensitive Hashing, LSH）的语义子空间投影机制，并确立其状态近似的理论误差上界。

定理 1（基于局部敏感哈希的语义流形误差界定）：给定边缘视觉联合特征集合 X ∈ R^N（本文中 N=529），若存在一个基于随机正交映射的哈希降维算子 f: R^N → R^k（本文提取核心语义代理 k=8），对于任意两个在原空间中极近的特征状态向量 u, v ∈ X，其在降维特征空间中的语义欧氏距离能够以概率 1-δ满足如下界限：
(1-ε)||u-v||² ≤ ||f(u)-f(v)||² ≤ (1+ε)||u-v||²

我们在保证ε< 0.15 的容忍度下，通过语义子空间投影构建了高维状态的一阶近似代理索引（Proxy Index）。即使大幅降维至 k=8，只要降维后的空间维度不低于该流形的本征维度（Intrinsic Dimensionality），原空间中具有高度意图相似性的样本（语义近邻）被哈希到同一索引桶的概率将呈指数级逼近 1。这不仅将知识库的搜索空间复杂度降低了数个数量级，更从数学上严密保证了哈希索引对底层核心物理经验的无损保真，以及下游意图推理的稳定性。

3.3  共享底层特征的 Actor-Critic 与知识融合机制

基于 529 维的联合状态特征，本文构建了适应边缘有限算力的共享底层双头（Twin-Head）网络结构。为了大幅降低参数量与推理时延，本文并未采用冗余的双网络分离架构，而是让策略头与价值头共享前置的特征抽象层。

输入与共享隐藏层（Shared Hidden Layers）：网络输入层直接接收 529 维的联合状态向量。隐藏层采用两层各包含 256 个神经元的全连接网络（Fully Connected Layers），并使用 ReLU 作为激活函数以增强网络的非线性特征提取能力。该共享层负责从高维输入中抽象出"语义 - 信道"的通用潜在表征。

Actor 策略头（Policy Head）：通过一个并行的线性全连接层输出维度为 3 的逻辑值，并经 Softmax 函数转化为合法的概率分布。该分布代表了智能体选择无损特征（Level 0）、适度压缩特征（Level 1）以及极限压缩特征（Level 2）的神经网络决策置信度。

Critic 价值头（Value Head）：通过另一个并行的线性神经元输出一个标量值 V(S)，用于评估当前本地状态的预期折扣回报，为策略的梯度更新提供低方差的基准线（Baseline）。

本文算法的最大创新在于将神经网络输出与知识库经验进行了数学层面的深度耦合。设策略网络输出的原始动作概率分布为 P_ppo，同时根据当前状态索引从知识库 K_i(t) 中提取对应的经验 Q 值并进行 Softmax 归一化，得到知识库建议分布 P_kb。

最终智能体执行的动作概率分布 P_final 由 PPO 策略分布与知识库经验分布的动态自适应融合决定：
P_final = (1 - α_t)·P_ppo + α_t·P_kb

摒弃传统工程上脆弱的静态硬编码（如常数设定），本文首创基于策略熵的动态微观信任评估机制。动态融合权重α_t 的解析式由当前 Actor 策略输出分布的信息熵 H(P_ppo) 与环境非平稳状态（信噪比 f_snr）联合驱动：
α_t = clip(γ·H(P_ppo) - η·f_snr, α_min, α_max)

该机制赋予了智能体卓越的微观动力学自适应能力：当面临信道环境剧烈突变或知识严重失配时，神经网络的决策不确定性激增（表现为 H(P_ppo) 显著放大），系统将自动拔高α_t 阈值，从而强化对知识库绝对先验经验的依赖以实现策略保底；反之，在稳态优质信道下，网络熵值收敛，智能体则自主降低α_t，释放深度强化学习的全局探索潜力。这一熵驱动的动态融合逻辑，从根本上消除了极端非平稳信道下的动作崩溃隐患。

3.4  联合优化奖励函数设计

为实现 6G 网络中极致推理精度与严苛传输代价的帕累托最优，本文突破传统的启发式惩罚建模，创新性地引入信息瓶颈（Information Bottleneck, IB）理论重构联合优化奖励函数。在知识演化范式下，智能体的决策本质上是在控制信息压缩的极值。我们将该优化目标严密定义为 IB 损失的最小化，即：

R = max(I(Z;Y) - β·I(X;Z))

其中，I(X;Z) 表征源端观测状态 X 压缩至特征 Z 所需的信道信息开销（物理上映射为计算与传输总时延）；β为拉格朗日乘子，用于调节通信代价与推理效用之间的折中；而 I(Z;Y) 表征降级后的语义特征 Z 与接收端下游推理意图 Y 之间的互信息量。

为了将抽象的互信息 I(Z;Y) 严密映射为可计算的工程量化指标，本文采用变分互信息下界（Variational Lower Bound）进行近似推导。根据信息论，互信息可展开为：
I(Z;Y) = H(Y) - H(Y|Z)

由于真实后验分布 p(Y|Z) 难以直接计算，我们引入变分近似分布 q_θ(Y|Z)。由 KL 散度的非负性可知 E_{Z,Y}[log p(Y|Z)] ≥ E_{Z,Y}[log q_θ(Y|Z)]，从而得到互信息的变分下界：
I(Z;Y) ≥ E_{Z,Y}[log q_θ(Y|Z)] + H(Y)

在语义文本评估中，设 Y 为真实意图，变分分布 q_θ(Y|Z) 衡量了在接收到降级语义 Z 时重构 Y 的似然概率。本文利用基于余弦相似度的 BERT-Score 作为该似然函数的经验代理（Empirical Surrogate），即假定 q_θ(Y|Z) ∝ exp(BERT-Score(Y, Ŷ))。在给定源端熵 H(Y) 为常数的前提下，最大化 BERT-Score 即等价于最大化 I(Z;Y) 的变分下界，从而保证了理论优化目标与实验评价指标的严密一致性。

3.5  基于 PPO 准则的智能体训练与知识同步更新

在明确了网络架构与奖励函数后，边缘感知智能体的底层参数更新严格遵循近端策略优化（PPO）的裁剪更新准则。区别于传统强化学习，本文算法在参数更新的同时，深度耦合了知识库的在线演化。具体的协同训练闭环包含以下四个核心步骤：

（1）环境交互与知识库在线演化（Data Collection & KB Update）在每个训练回合（Episode）中，智能体依托当前的策略网络与时变信道交互 T 步（系统设定为 100 步）。在每一步决策后，智能体不仅将状态转移元组 (s_t, a_t, r_t, s_{t+1}, log π_θ) 存入经验回放池，同时立即触发本地语义知识库的更新机制：利用提取的哈希降维索引，结合当前步的奖励反馈 r_t，对知识库状态矩阵 K_i(t) 进行在线平滑更新，实现物理环境交互与逻辑知识沉淀的同步进行。

（2）广义优势函数评估（GAE Calculation）当经验池收集满当前回合的数据后，利用 Critic 价值头评估的基准价值 V_φ(s_t)，计算衡量当前动作优于平均表现的优势函数 A_t。为精准权衡方差与偏差，系统采用广义优势估计（GAE）方法，并严格设定折扣因子γ = 0.99 与衰减因子λ = 0.95 进行多步回报的优势计算。

（3）网络参数的信任域裁剪更新（Clipping Update）为避免神经网络步长过大导致的策略崩溃，采用批量化（Mini-batch size = 64）、多轮次（Epochs = 10）的梯度下降。Actor 策略头的更新采用 PPO 专属的裁剪（Clipping）目标函数。定义新旧策略概率比为 r_t(θ)，策略优化的目标函数最大化下式：
L^{CLIP}(θ) = E[min(r_t(θ)A_t, clip(r_t(θ), 1-ε, 1+ε)A_t)]
其中ε为裁剪超参数（系统设定为 0.2）。该机制将策略的更新幅度强制限制在信任域（Trust Region）内，确保了探索与学习的单调收敛性。

（4）综合损失函数计算（Total Loss Optimization）最终驱动整个双头网络梯度下降的总损失函数（Total Loss）由三部分加权构成：
L_total = -L^{CLIP}(θ) + c_1 L^{VF}(φ) - c_2 S[π_θ](s_t)
其中，L^{VF}(φ) 为 Critic 价值头预测值与实际回报之间的均方误差（MSE）；S[π_θ] 为策略输出分布的信息熵（Entropy Bonus），用于鼓励智能体在恶劣网络中进行充分探索以避免陷入局部最优。在仿真实现中，价值系数设定为 c_1 = 0.5，熵激励系数设定为 c_2 = 0.05。

4  实验设计与数据分析

4.1  实验场景与数据集构建

为验证本文所提智能体在复杂视觉场景下的泛化能力与语义推理效能，实验基础数据集选用权威的 MS COCO 2017 Validation Set。从中针对性地筛选了 1000 张涵盖典型 6G 边缘物联网场景（如城市交通监控、人群密集广场、复杂工业设备等）的高价值图像子集。

在语义标签的构建上，有别于传统仅依赖单一标签的分类任务，本文借助自然语言处理句法树解析工具（Stanford CoreNLP），对图像的 Ground Truth Caption 进行了基于依存句法分析的自动化处理，创新性地构建了满足不同传输带宽约束的"三级递进式语义表示"：
- Level 0（细粒度全量语义）：保留图像的完整上下文描述，包含所有实体特征、状态修饰词及空间关系。该层级意图互信息 I(Z;Y) 保真度极高，但所需表征的 Token 数量庞大。
- Level 1（结构化骨架语义）：对冗余修饰成分进行剪枝，仅提取句法树中的核心主谓宾（SVO）三元组。该层级实现了语义信息保留与数据开销的帕累托适度折中。
- Level 2（极简核心实体）：执行极限信息瓶颈压缩，仅保留画面中最关键的独立目标实体标签。该层级旨在极其恶劣的信道状态下，以极小的数据量保障系统最基础的感知可用性。

4.2  物理层参数与算力时延标定

通信信道模型：实验模拟了面向 6G 边缘智联的典型 Sub-6GHz/毫米波宽带传输链路，系统可用带宽设定为 100 MHz。信道衰落建模为准静态平坦衰落信道，其接收端信噪比（SNR）的波动范围设定为 -10 dB 至 20 dB，以全面覆盖从深度衰落拥塞到视距优质传输（LoS）的各类极端动态网络状态。

在系统工程部署范式上，KE-PPO 智能体采用'云端/MEC 离线集中训练，边缘在线分布式执行'的异步架构。对于具备高维度连续状态空间的双头 Actor-Critic 网络，其模型训练阶段部署于 6G 边缘汇聚服务器（MEC）的高并行张量加速集群中。而在资源极度受限的感知端节点（标定为 15 TOPS 算力），仅需执行轻量级的网络前向推理与基于 LSH 的哈希字典查表更新，其在线演化的绝对算力开销处于亚毫秒级（Sub-millisecond），完美契合 6G 网络对超低时延的严苛物理约束。

4.3  实验结果分析

4.3.1  算法收敛性与系统能效对比

为了验证本文提出的协同推理智能体在边缘网络环境下的基础性能与收敛效果，本节在固定中等优质信噪比（10 dB）且施加严苛时延惩罚（0.0015）的环境下，开展了算法收敛性与系统整体能效的对比评估。

对比算法与基线设定：
- Greedy（贪婪策略）：始终选择传输未经任何压缩的全量图像特征（Level 0），代表追求最高单步推理精度的理论性能上限。
- Random（随机策略）：在三个语义压缩等级中进行无差别随机采样，作为系统性能的托底基线。
- Pure PPO（纯近端策略优化，即 No-KB）：采用与本文一致的双头网络架构，但剥离动态知识库（SKB）引导，完全依赖数据驱动在非平稳环境中探索。
- Static-KB（静态知识库）：保留知识库但采用僵化的固定加权融合规则（α=0.3），用于验证动态自适应机制的必要性。
- KE-PPO（本文算法）：采用策略熵动态融合机制与自演化知识库的完整协同架构。

评估指标：
- 平均累计奖励（Reward）：综合表征智能体在长序列决策任务中，平衡"高语义保真度"与"低传输时延"的联合优化能力。
- 系统能效（Efficiency）：定义为单次推理任务中"准确率/时延（Acc/Delay）"的比值。该指标直观量化了单位时间开销下系统所获取的有效语义信息增益，是衡量 6G 绿色计算效能的核心指标。

实验结果表明，本文提出的 KE-PPO 策略在平均累计奖励上取得了显著的性能优势。具体而言，KE-PPO 奖励值稳定在 44.4 左右，远超 Pure PPO 基线（33.6）和 Greedy 策略（33.3）。这一结果揭示了两个深层逻辑：
（1）知识引导的必要性：Pure PPO 的表现几乎与 Greedy 策略持平，甚至在非平稳探索初期表现不佳，证明了在面对高维视觉特征与时变信道时，纯数据驱动的 DRL 算法极易陷入局部最优或探索平台期。
（2）KE 机制的卓越提效：引入 SKB 知识库后（KE-PPO 与 Static-KB），系统收益实现了约 32% 的阶跃式增长。这有力地证明了 LSH 哈希字典沉淀的平稳先验经验能够有效指导智能体避开高时延、低收益的决策陷阱。

在系统能效方面，KE-PPO 录得 0.0045 的能效值，虽在固定 SNR 场景下略低于手工调优的 Static-KB（0.0053），但相比 Greedy 和 Pure PPO（均约为 0.0024）实现了近 2 倍的能效打击。这表明 KE-PPO 并非通过机械的过度压缩来换取时延，而是通过动态识别视觉特征的意图价值，在维持高保真度的前提下极大地缩减了冗余传输开销。

4.3.2  真实视觉场景下的极端信道鲁棒性与消融分析

在完成理论机理验证后，本节将真实 MS COCO 验证集与轻量级 ResNet 前端引入评估环境，并设置连续变化的物理信道信噪比（SNR ∈ [-10, 20] dB），以映射 6G 边缘网络中时变衰落的典型动态特征。为全面评估所提机制的综合效能，本实验在联合时延惩罚的约束下，重点考察不同决策架构的平均推理准确率（BERT-Score）演化趋势。

通过消融基线的横向对比，本文进一步验证了核心创新模块的有效性：
（1）先验知识库的引导作用：剥离了知识库的强化学习基线（No-KB, 纯 PPO）在全频段内的准确率均出现明显衰退。这揭示了在缺乏先验经验引导时，纯数据驱动算法在应对高维非平稳环境时存在一定的探索局限性，从侧面印证了动态语义知识库（SKB）对提升决策稳定性和探索效率具有积极作用。
（2）动态熵驱动机制的必要性：实验引入了采用固定加权常数的静态知识库策略（Static-KB, α=0.3）。结果显示，该刚性策略在复杂信道下的表现欠佳，其性能在优质信道区间甚至低于纯 PPO 算法，且不及启发式的单级压缩策略（Static, Level 1）。该现象表明，僵化的超参数设定难以适应带宽的动态扩张与收缩；而本文所提"基于策略熵的动态融合机制"能够更合理地量化决策不确定性，促使智能体在"信任先验经验"与"自主探索网络"之间达成较好的动态平衡，有效缓解了局部非平稳条件下的动作崩溃现象。

4.3.3  LSH 语义知识库的演化动力学分析

为了深入探究动态知识库（SKB）在底层状态空间的演变规律，本节重点剖析了知识库容量的宏观演化动力学特征。实验记录了智能体在全频段时变信道交互中，其内部 LSH 状态哈希字典的独立状态覆盖率随训练回合（Episode）的增长轨迹。

实验结果表明，知识库的状态覆盖率呈现出典型的对数型（Logarithmic）收敛趋势。在训练初期的前 200 个回合内，曲线斜率较大，表明智能体处于高频的探索阶段，持续将未知的高价值"信道 - 语义"联合物理状态通过 LSH 算子映射并沉淀至本征知识子空间；随着训练推进至 400 回合以后，曲线逐步趋于渐进稳定，形成约 15000 个独立状态的饱和平台。

这一从"高频积累"到"渐进饱和"的闭环过程，从物理系统论的层面严密验证了 LSH 降维算子的保距性与收敛有界性。实验表明，该动态知识库能够在严格控制内存与维度开销的前提下，高效捕获环境的底层核心规律，为处于高维非平稳环境中的边缘智能体提供稳定可靠的底层决策边界。

4.3.4  高度非平稳环境下的熵驱动微观动力学

通过记录智能体在持续注入外部动态扰动时的内部参数变化，微观演化图谱清晰地展现了α_t 的非平稳波动特征。实验发现，α_t 并未坍缩为单一常数，而是与系统内部的信息熵 H(P_ppo) 以及接收端的知识失配度（KL 散度）形成了高度的数理协同响应。需要强调的是，尽管α_t 的绝对波动区间在 0.44 至 0.58 之间，但在 Softmax 指数归一化空间中，0.14 的权重偏移足以导致动作输出概率分布发生数量级的倾斜，从而在极短的决策步长内实现探索与利用边界的瞬时反转，有效规避动作崩溃。

具体而言，当物理环境发生剧烈衰落或知识对齐发生严重失配时，神经网络难以通过局部侦测输出高置信度动作，其策略输出分布的信息熵瞬间激增。此时，底层机制敏锐捕捉到这一"决策迷茫期"，自适应拉升了融合权重α_t（曲线呈现显著峰值），促使智能体主动依赖本地 LSH 知识库中沉淀的平稳先验经验，从而实施了边界风险规避；反之，在网络状态进入优质稳态期时，网络熵值回落，α_t 自动衰减至低位，智能体重新释放出主动探索高阶语义表征的潜力。这一熵驱动的动态微观寻优机制，揭示了 KE-PPO 算法有效规避非平稳陷阱、实现对 6G 极端动态环境深度自适应的底层逻辑。

5  结束语

本文针对 6G 语义通信中环境波动与边缘知识失配带来的挑战，研究了面向深层语义智能体的知识演化与自适应传输机制。本文创新性地引入局部敏感哈希（LSH）算子确立了特征降维的误差界限，并将系统通信决策重构为面向感知节点的 POMDP。在此基础上，提出了融合信息瓶颈（IB）理论与熵驱动动态融合权重的 KE-PPO 算法。该机制使得边缘智能体能够有效规避高昂的全局信令交互，仅依赖局部物理感知与底层知识库的对数型时序演化，即可自主输出环境适配的多级语义传输策略。

结合机理仿真与真实视觉数据集的四维实验矩阵，全面验证了所提机制的理论有效性与综合性能优势。在理论维度，KE-PPO 机制有效逼近了全局完美感知下的帕累托最优前沿；在物理应用维度，凭借熵驱动的微观动力学演化，智能体在极端衰落信道下展现出灵活的"策略跃迁"能力，显著改善了传统纯强化学习的探索震荡与静态规则的超时惩罚问题。本研究从信息论视角探讨了推理精度与通信代价的联合优化路径，为未来 6G 边缘智联网络中轻量级、意图驱动的多节点协同提供了严密的理论参考与可行的演进范式。

参考文献：
[1] STRINATI E C, BARBAROSSA S, GONZALEZ-JIMENEZ J L, et al. 6G networks: Beyond Shannon towards semantic and goal-oriented communications[J]. Computer Networks, 2021, 190: 107930.
[2] 张平，牛凯，田辉，等。语义通信：6G 智能化网络的新范式 [J]. 移动通信，2021, 45(6): 1-12.
[3] ZHANG P, XU W, GAO H, et al. Toward wisdom-evolutionary and primitive-concise 6G: A new paradigm of semantic communication networks[J]. Engineering, 2022, 8: 60-73.
[4] XIE H, QIN Z, LI G Y, et al. Deep learning enabled semantic communication systems[J]. IEEE Transactions on Signal Processing, 2021, 69: 2663-2675.
[5] YANG W, DU H, LIEW S C, et al. Semantic communications for future internet: Fundamentals, applications, and challenges[J]. IEEE Communications Surveys & Tutorials, 2023, 25(1): 213-250.

[作者简介]
诸葛斌（1976-），男，博士，浙江工商大学信息与电子工程学院（萨塞克斯人工智能学院）教授，主要研究方向为网络与通信技术、互联网技术和网络安全。
洪仕玉（2002-），女，硕士研究生，浙江工商大学信息与电子工程学院（萨塞克斯人工智能学院），主要研究方向为数据资源调度、智慧网络。
许云汉（2000-），男，硕士研究生，浙江工商大学信息与电子工程学院（萨塞克斯人工智能学院），主要研究方向为云计算与资源调度。
董黎刚（1973-），男，博士，浙江工商大学信息与电子工程学院（萨塞克斯人工智能学院）教授，IEEE 和 IEEE-CS 成员，中国电子学会高级会员，浙江计算机学会理事，主要研究方向为基于大数据与深度学习的智慧网络与智慧教育及分布式系统理论。
张子天（1988-），男，博士，浙江工商大学信息与电子工程学院（萨塞克斯人工智能学院）副研究员，主要研究方向为基于人工智能的网络流量预测与资源管理。
