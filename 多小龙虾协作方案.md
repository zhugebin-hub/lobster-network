# 🦞 多小龙虾钉钉群协作方案

> 基于文章《OpenClaw 实战：一个人、一台 Mac、六个 AI Agent》的多 Agent 协作经验，
> 结合钉钉群场景定制。

---

## 📖 文章核心发现

原文作者用 OpenClaw 实现了 **6 个 AI Agent 在 Discord 上的协作**（Zoe 管家、Macro 宏观分析、Trading 交易蜘蛛、Content 内容蜘蛛、ainews 情报蜘蛛、Butler 管家），关键结论：

> **"协作是协议问题，不是群聊问题"**
> 两个 Agent 放在同一个群不写协议，等价于两个进程共享内存不加锁。

---

## 🏗️ 方案架构

### 方案概览

```
┌─────────────────────────────────────────────┐
│              钉钉群（人类可见）               │
│  诸葛斌  孙豪  小龙虾A  小龙虾B  小龙虾C     │
└──────────────┬──────────────────────────────┘
               │ @触发 / 群消息
               ▼
┌─────────────────────────────────────────────┐
│          OpenClaw Gateway（一台机器）         │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│  │ 小龙虾A  │ │ 小龙虾B  │ │ 小龙虾C  │       │
│  │ (agent) │ │ (agent) │ │ (agent) │       │
│  └────┬────┘ └────┬────┘ └────┬────┘       │
│       │           │           │              │
│  ┌────┴───────────┴───────────┴────┐        │
│  │       shared-context/           │        │
│  │    (共享工作区，文件级通信)       │        │
│  └─────────────────────────────────┘        │
└─────────────────────────────────────────────┘
```

---

## 🔧 实施方案（从简到繁，三个阶段）

### 阶段一：群内 @ 对话（立即可用，0 配置）

**原理**：每只小龙虾是独立的钉钉机器人，通过 `@` 触发。

**配置**：
- 每只小龙虾 = 一个 OpenClaw agent（独立 workspace）
- 都接入同一个钉钉群
- 设置 `requireMention: true`（只响应 @自己的消息）

**优点**：
- ✅ 最简单，立即可用
- ✅ 人类可看到全部对话
- ✅ 不需要额外开发

**缺点**：
- ❌ 容易产生 ACK storm（互相 @ 到死）
- ❌ 消息刷屏打扰人类
- ❌ 不适合高频协作

**防刷屏规则**（写入每个小龙虾的 SOUL.md）：
```
🚫 禁止在群聊中对其他机器人的消息回复"收到/OK/感谢"
✅ 只在被人类 @ 或被明确指派任务时才回复
✅ 如需确认其他小龙虾的消息，使用 NO_REPLY
```

---

### 阶段二：三态通信协议 + 共享工作区（推荐）

这是文章作者验证过的核心方案。

#### 2.1 三态通信协议

每次协作有且仅有三个状态：

| 状态 | 格式 | 说明 |
|------|------|------|
| `request` | `@对方 [state=request] [ack_id=主题-v1-20260410] 期望动作` | 发起任务 |
| `confirmed` | `@发起方 [state=confirmed] [ack_id=...] 关键结论` | 确认/回复 |
| `final` | `@相关方 [state=final] [ack_id=...] 终态结论` | 最终收敛 |

**核心规则**：
- `final` 后全员静默，禁止"收到/感谢/OK"
- 同一线程只允许一个 `ack_id`
- 5 分钟无 `confirmed` → 催办 1 次
- 10 分钟仍无 → 升级给人类仲裁

#### 2.2 shared-context/ 共享工作区

```
shared-context/
├── intel/              # 情报共享
│   ├── topic_A.json    # 小龙虾A的产出
│   └── topic_B.json    # 小龙虾B的产出
├── status/             # 各 Agent 当前状态
│   ├── 小龙虾A.json
│   └── 小龙虾B.json
├── roundtable/         # 圆桌讨论记录
├── decisions/          # 重大决策存档
└── PROJECT_STATUS.md   # 项目全局状态
```

**核心价值**：从消息驱动升级到状态驱动。
- 小龙虾B 不需要每次问小龙虾A"你分析完了没"
- 直接读 `shared-context/intel/topic_A.json`
- `sessions_send` 适合实时触发，但不可靠（超时、重复）
- 关键数据走文件才可追溯

#### 2.3 两种通信机制对比

| 机制 | 用途 | 例子 |
|------|------|------|
| 群消息 @ | 实时任务分派 | `@十三香小龙虾 帮我查下今天的菜谱` |
| shared-context/ 文件 | 异步状态共享 | 小龙虾A 写入文件 → 小龙虾B 读取 |

---

### 阶段三：完整编排（进阶）

#### 3.1 角色分工

参考文章的 6 Agent 阵型，小龙虾群可以这样分工：

| 角色 | 名称 | 职责 |
|------|------|------|
| 协调员 | Zoe小龙虾 | 发起圆桌、汇总结果、仲裁争议 |
| 执行者A | 十三香小龙虾 | 专注十三香菜谱/知识 |
| 执行者B | 蒜蓉小龙虾 | 专注蒜蓉菜谱/知识 |
| 执行者C | 豪虾 | 通用问答 + 文件处理 |
| 执行者D | 诸葛虾 | 技术教程 + 代码相关 |
| 情报员 | uu虾 | 信息收集 + 趋势分析 |

#### 3.2 圆桌讨论流程

```
Zoe小龙虾 发起议题
  ↓
@十三香小龙虾 [state=request] [ack_id=菜谱-v1] 提供十三香做法
  ↓
十三香小龙虾 → [state=confirmed] [ack_id=菜谱-v1] 输出做法
  ↓
@蒜蓉小龙虾 [state=request] [ack_id=菜谱-v1] 提供蒜蓉做法
  ↓
蒜蓉小龙虾 → [state=confirmed] [ack_id=菜谱-v1] 输出做法
  ↓
Zoe小龙虾 → [state=final] [ack_id=菜谱-v1] 汇总对比 + 打包发送
  ↓
全员静默 ✅
```

---

## 📋 具体实施步骤

### Step 1：为每只小龙虾配置独立 workspace

```bash
# 每只小龙虾有自己的 openclaw agents
openclaw agents create 十三香小龙虾
openclaw agents create 蒜蓉小龙虾
openclaw agents create 豪虾
# ... 更多
```

### Step 2：配置钉钉渠道

每只小龙虾需要：
- 独立的钉钉机器人 AppKey/AppSecret
- 独立的 openclaw.json 配置
- `requireMention: true`（只在被 @ 时响应）

### Step 3：写入通信协议

在每个小龙虾的 AGENTS.md 中写入：

```markdown
## 群聊通信规则

### 三态协议（强制）
[request]   @对方 + ack_id + 期望动作 + 截止时间
[confirmed] @发起方 + 相同 ack_id + 版本号/关键结论
[final]     @相关方 + 相同 ack_id + 终态收敛

### 禁止行为
- final 后禁止续话
- 禁止回复"收到/感谢/OK"
- 同一 ack_id 不得重试超过 1 次

### 超时处理
- 5 分钟无 confirmed → 催办 1 次
- 10 分钟仍无 → 升级 Zoe 仲裁
```

### Step 4：创建 shared-context/ 目录

```bash
mkdir -p shared-context/{intel,status,roundtable,decisions}
```

### Step 5：测试

1. 在群里 `@小龙虾A` 发起一个任务
2. 观察是否正确响应
3. 测试 `@小龙虾B` 让 B 补充
4. 观察是否有 ACK storm
5. 调整协议直到稳定

---

## ⚠️ 文章中的血泪教训

1. **Agent 不回复是常态** — 准备好 Task Watcher 和重试机制
2. **投递成功 ≠ 归档成功** — 无报错 ≠ 有产出
3. **规则用最强措辞** — LLM 对"建议"的遵循率远低于"MUST"
4. **每加一个 Agent 都需要半天调试** — 急于求成 = 浪费更多时间
5. **shared-context/ 是协作基石** — sessions_send 不可靠，关键数据走文件
6. **持续运行的系统必然退化** — 需要反退化机制

---

## 🎯 推荐路径

| 时间 | 目标 |
|------|------|
| 第 1 天 | 先让 1 只小龙虾（你自己）在群里稳定运行 |
| 第 2-3 天 | 加第 2 只小龙虾，配置 requireMention，测试 @ 交互 |
| 第 4-5 天 | 写入三态协议，创建 shared-context/，测试协作 |
| 第 2 周 | 逐步扩展到完整阵型，每加一只调试半天 |

---

**总结**：多小龙虾协作的核心不是"拉个群让它们聊"，而是**设计协议 + 共享状态 + 角色分工**。

文章原文：[阿里云开发者 - OpenClaw 实战](https://mp.weixin.qq.com/s/S6XKuXHa4QqZ_qno1yGIFQ)
