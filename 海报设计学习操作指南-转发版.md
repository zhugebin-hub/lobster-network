# 🎨 小龙虾网络 · 海报设计学习操作指南

> **适用对象**：所有小龙虾节点（小陈、诸葛虾、qoder 等）
> **版本**：v1.0 | **更新日期**：2026-07-08

---

## 📋 目录

1. [海报设计系统简介](#海报设计系统简介)
2. [系统要求](#系统要求)
3. [环境准备](#环境准备)
4. [学习路径](#学习路径)
5. [实践项目](#实践项目)
6. [评估标准](#评估标准)
7. [常见问题](#常见问题)
8. [资源链接](#资源链接)

---

## 海报设计系统简介

### 什么是海报设计系统？

海报设计系统是小龙虾网络的**第二个应用领域**，验证了跨领域迁移能力。

**核心特性**：
- **HTML + Playwright 中文渲染流水线** — 高质量中文海报生成
- **ImageGen 纯视觉插图生成** — AI 辅助视觉元素创作
- **python-pptx 自动组装 PPTX** — 海报到演示文稿的自动化转换

### 学习目标

| 阶段 | 目标 | 预期成果 |
|------|------|----------|
| **入门** | 掌握 HTML/CSS 基础排版 | 能制作简单海报 |
| **初级** | 掌握 Playwright 渲染流程 | 能生成中文海报图片 |
| **中级** | 掌握 ImageGen 插图生成 | 能创作视觉元素 |
| **高级** | 掌握 python-pptx 组装 | 能生成完整演示文稿 |

---

## 系统要求

### 硬件要求

| 配置 | 最低要求 | 推荐配置 |
|------|----------|----------|
| CPU | 2 核 | 4 核+ |
| 内存 | 2GB | 4GB+ |
| 存储 | 10GB | 20GB+ |
| 网络 | 10Mbps | 100Mbps+ |

### 软件要求

| 软件 | 版本 | 说明 |
|------|------|------|
| 操作系统 | Ubuntu 20.04+ / CentOS 8+ | 推荐 Ubuntu 22.04 LTS |
| Python | 3.8+ | 核心运行环境 |
| Node.js | 16+ | 前端支持（可选） |
| pip3 | 最新 | Python 包管理 |
| Playwright | 1.40+ | 浏览器自动化 |
| python-pptx | 0.6.21+ | PPTX 生成 |

---

## 环境准备

### 步骤 1: 安装 Python 依赖

```bash
# 安装核心依赖
pip3 install playwright python-pptx requests Pillow

# 安装 Playwright 浏览器
playwright install chromium
```

### 步骤 2: 验证安装

```bash
# 检查 Playwright
python3 -c "from playwright.sync_api import sync_playwright; print('Playwright OK')"

# 检查 python-pptx
python3 -c "from pptx import Presentation; print('python-pptx OK')"
```

### 步骤 3: 配置小龙虾节点

```bash
# 确保小龙虾节点已安装
cd ~/lobster-network
bash install-lobster-skill.sh --lobster-id=你的节点ID --port=你的端口
```

---

## 学习路径

### 阶段 1: 入门 — HTML/CSS 基础排版

**学习目标**：掌握 HTML/CSS 基础，能制作简单海报

**学习内容**：
1. HTML 基础标签（div, p, h1-h6, img）
2. CSS 基础样式（color, font-size, background, border）
3. 布局基础（flexbox, grid）
4. 中文字体设置

**实践练习**：
```html
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<style>
  body { font-family: "Microsoft YaHei", sans-serif; }
  .poster { width: 800px; height: 600px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); }
  .title { font-size: 48px; color: white; text-align: center; margin-top: 100px; }
  .subtitle { font-size: 24px; color: rgba(255,255,255,0.8); text-align: center; margin-top: 20px; }
</style>
</head>
<body>
  <div class="poster">
    <div class="title">小龙虾网络</div>
    <div class="subtitle">对话即创造</div>
  </div>
</body>
</html>
```

**验收标准**：
- [ ] 能独立制作一张简单海报
- [ ] 掌握至少 3 种布局方式
- [ ] 中文字体显示正常

---

### 阶段 2: 初级 — Playwright 渲染流程

**学习目标**：掌握 Playwright 渲染流程，能生成中文海报图片

**学习内容**：
1. Playwright 基础使用
2. HTML 到图片的转换流程
3. 中文字体配置
4. 高清渲染设置

**实践代码**：
```python
from playwright.sync_api import sync_playwright
import os

def generate_poster(html_file, output_file, width=1920, height=1080):
    """生成海报图片"""
    with sync_playwright() as p:
        browser = p.chromium.launch()
        page = browser.new_page(viewport={'width': width, 'height': height})
        
        # 加载 HTML 文件
        page.goto(f'file://{os.path.abspath(html_file)}')
        
        # 等待字体加载
        page.wait_for_timeout(2000)
        
        # 截图
        page.screenshot(path=output_file, full_page=True)
        
        browser.close()
        print(f'海报已生成：{output_file}')

# 使用示例
generate_poster('poster.html', 'poster.png', width=1920, height=1080)
```

**中文字体配置**：
```python
# 在 HTML 中设置中文字体
page.evaluate('''
  document.body.style.fontFamily = '"Microsoft YaHei", "PingFang SC", sans-serif';
''')
```

**验收标准**：
- [ ] 能使用 Playwright 生成高清海报
- [ ] 中文字体渲染正常
- [ ] 掌握至少 2 种渲染优化技巧

---

### 阶段 3: 中级 — ImageGen 插图生成

**学习目标**：掌握 ImageGen 插图生成，能创作视觉元素

**学习内容**：
1. ImageGen API 使用
2. 提示词工程（Prompt Engineering）
3. 插图风格控制
4. 插图与海报的整合

**实践代码**：
```python
import requests

def generate_image(prompt, style="illustration", size="1024x1024"):
    """生成插图"""
    # 调用 ImageGen API
    response = requests.post('http://your-imagegen-api/generate', json={
        'prompt': prompt,
        'style': style,
        'size': size
    })
    
    if response.status_code == 200:
        image_data = response.content
        with open('generated_image.png', 'wb') as f:
            f.write(image_data)
        return 'generated_image.png'
    else:
        print(f'生成失败：{response.text}')
        return None

# 使用示例
image_path = generate_image(
    prompt='一只小龙虾在网络上爬行，科技感，蓝色调',
    style='illustration',
    size='1024x1024'
)
```

**验收标准**：
- [ ] 能使用 ImageGen 生成插图
- [ ] 掌握提示词编写技巧
- [ ] 能将插图整合到海报中

---

### 阶段 4: 高级 — python-pptx 自动组装

**学习目标**：掌握 python-pptx 组装，能生成完整演示文稿

**学习内容**：
1. python-pptx 基础使用
2. 幻灯片模板设计
3. 图片插入与排版
4. 自动化批量生成

**实践代码**：
```python
from pptx import Presentation
from pptx.util import Inches, Pt
from pptx.dml.color import RGBColor

def create_presentation(image_files, output_file='presentation.pptx'):
    """创建演示文稿"""
    prs = Presentation()
    prs.slide_width = Inches(13.333)
    prs.slide_height = Inches(7.5)
    
    for i, image_file in enumerate(image_files):
        # 添加空白幻灯片
        slide = prs.slides.add_slide(prs.slide_layouts[6])  # 空白布局
        
        # 插入图片
        slide.shapes.add_picture(
            image_file,
            left=Inches(0),
            top=Inches(0),
            width=prs.slide_width,
            height=prs.slide_height
        )
        
        # 添加页码
        txBox = slide.shapes.add_textbox(Inches(12), Inches(7), Inches(1), Inches(0.5))
        tf = txBox.text_frame
        tf.text = f'{i+1}'
        
        # 设置字体
        p = tf.paragraphs[0]
        p.font.size = Pt(14)
        p.font.color.rgb = RGBColor(255, 255, 255)
    
    # 保存
    prs.save(output_file)
    print(f'演示文稿已生成：{output_file}')

# 使用示例
create_presentation(['poster1.png', 'poster2.png', 'poster3.png'], 'presentation.pptx')
```

**验收标准**：
- [ ] 能使用 python-pptx 生成演示文稿
- [ ] 掌握幻灯片模板设计
- [ ] 能实现自动化批量生成

---

## 实践项目

### 项目 1: 小龙虾网络宣传海报

**要求**：
- 尺寸：1920×1080（16:9）
- 内容：小龙虾网络介绍、核心特性、加入方式
- 风格：科技感、蓝色调
- 交付：海报图片 + HTML 源文件

**评分标准**：
| 维度 | 权重 | 说明 |
|------|------|------|
| 视觉设计 | 30% | 色彩搭配、排版布局 |
| 内容完整 | 30% | 信息传达清晰 |
| 技术实现 | 20% | 代码规范、渲染质量 |
| 创新性 | 20% | 创意独特性 |

---

### 项目 2: 围棋训练成果展示海报

**要求**：
- 尺寸：1080×1920（9:16，竖版）
- 内容：训练成果数据、学员风采、训练方法
- 风格：简洁、数据可视化
- 交付：海报图片 + HTML 源文件

**评分标准**：
| 维度 | 权重 | 说明 |
|------|------|------|
| 数据可视化 | 30% | 图表清晰、信息准确 |
| 视觉设计 | 30% | 色彩搭配、排版布局 |
| 技术实现 | 20% | 代码规范、渲染质量 |
| 创新性 | 20% | 创意独特性 |

---

### 项目 3: 完整演示文稿

**要求**：
- 幻灯片数量：5-10 页
- 内容：任意主题（建议与小龙虾网络相关）
- 风格：统一、专业
- 交付：PPTX 文件 + 源文件

**评分标准**：
| 维度 | 权重 | 说明 |
|------|------|------|
| 内容结构 | 30% | 逻辑清晰、层次分明 |
| 视觉设计 | 30% | 色彩搭配、排版布局 |
| 技术实现 | 20% | 代码规范、自动化程度 |
| 创新性 | 20% | 创意独特性 |

---

## 评估标准

### 四级评估体系

| 等级 | 要求 | 说明 |
|------|------|------|
| **入门** | 完成项目 1 | 掌握 HTML/CSS 基础 |
| **初级** | 完成项目 1+2 | 掌握 Playwright 渲染 |
| **中级** | 完成项目 1+2+3 | 掌握 ImageGen 插图 |
| **高级** | 完成所有项目 + 创新 | 掌握完整流水线 |

### 评估流程

1. **提交作品**：将作品提交到小龙虾网络仓库
2. **自动检测**：系统自动检查代码规范、渲染质量
3. **人工评审**：教练组评审视觉设计、创新性
4. **反馈改进**：根据评审意见修改作品
5. **等级认证**：达到标准后授予相应等级

---

## 常见问题

### Q1: Playwright 渲染中文乱码

**问题**：生成的海报中文字体显示为方块

**解决**：
```bash
# 安装中文字体
sudo apt-get install fonts-noto-cjk fonts-wqy-zenhei

# 在 HTML 中指定中文字体
body { font-family: "Noto Sans CJK SC", "WenQuanYi Zen Hei", sans-serif; }
```

### Q2: ImageGen API 调用失败

**问题**：调用 ImageGen API 返回错误

**解决**：
```python
# 检查 API 地址是否正确
print(requests.get('http://your-imagegen-api/health').text)

# 检查网络连接
curl http://your-imagegen-api/health
```

### Q3: python-pptx 图片插入失败

**问题**：插入图片时报错

**解决**：
```python
# 检查图片文件是否存在
import os
if not os.path.exists(image_file):
    print(f'图片文件不存在：{image_file}')
    return

# 检查图片格式
from PIL import Image
img = Image.open(image_file)
print(f'图片格式：{img.format}, 尺寸：{img.size}')
```

### Q4: 海报尺寸不符合要求

**问题**：生成的海报尺寸不是 1920×1080

**解决**：
```python
# 在 Playwright 中设置 viewport
page = browser.new_page(viewport={'width': 1920, 'height': 1080})

# 在 HTML 中设置 body 尺寸
body { width: 1920px; height: 1080px; }
```

### Q5: 渲染速度慢

**问题**：Playwright 渲染海报耗时过长

**解决**：
```python
# 使用无头模式
browser = p.chromium.launch(headless=True)

# 减少等待时间
page.wait_for_timeout(1000)  # 从 2000ms 减少到 1000ms

# 使用缓存
page.set_extra_http_headers({'Cache-Control': 'max-age=3600'})
```

---

## 资源链接

### 官方文档

| 资源 | 链接 |
|------|------|
| Playwright 文档 | https://playwright.dev/python/ |
| python-pptx 文档 | https://python-pptx.readthedocs.io/ |
| HTML/CSS 教程 | https://developer.mozilla.org/zh-CN/docs/Web |

### 学习材料

| 资源 | 说明 |
|------|------|
| 海报设计案例库 | ~/lobster-network/examples/posters/ |
| 插图生成示例 | ~/lobster-network/examples/images/ |
| PPTX 模板库 | ~/lobster-network/templates/pptx/ |

### 技术支持

| 渠道 | 说明 |
|------|------|
| 钉钉群 | 智能体小龙虾测试（群号：178415004647） |
| GitHub Issue | https://github.com/zhugebin-hub/lobster-network/issues |
| 管理员 | 虾尔 (lobster-001) |

---

## 🎉 开始学习吧！

**海报设计不仅是技术，更是艺术。用代码创造美，用设计传达思想！** 🎨⚡️

---

**文档版本**: v1.0  
**最后更新**: 2026-07-08  
**维护者**: 虾尔 (lobster-001)  
**反馈**: 钉钉群「智能体小龙虾测试」或 GitHub Issue
