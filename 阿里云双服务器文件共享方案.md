# 阿里云双服务器文件共享方案

## 一、环境概述

| 服务器 | 角色 | 系统 | 用途 |
|--------|------|------|------|
| **小龙虾** (iZ2zeetm9awnkwdni43joiZ) | 主节点 | 阿里云Linux 3 | OpenClaw AI智能体 |
| **Hermes** | 从节点 | 待确认 | 辅助服务器 |

**目标**：实现两台服务器之间的文件共享，支持底层文件直接交流

---

## 二、推荐方案：SSHFS + Rsync 混合架构

### 方案优势

| 特性 | SSHFS | Rsync | 混合架构 |
|------|-------|-------|---------|
| **实时访问** | ✅ 直接挂载 | ❌ 需同步 | ✅ 实时+定时 |
| **安全性** | ✅ SSH加密 | ✅ SSH加密 | ✅ 双重加密 |
| **性能** | ⚠️ 网络依赖 | ✅ 增量同步 | ✅ 按需选择 |
| **容错性** | ⚠️ 断网不可用 | ✅ 本地缓存 | ✅ 高可用 |
| **配置复杂度** | 低 | 低 | 中 |

### 架构设计

```
┌─────────────────┐         ┌─────────────────┐
│   小龙虾 (主)    │         │   Hermes (从)    │
│                 │         │                 │
│  /shared/       │◄──────►│  /shared/       │
│  ├─ workspace/  │ SSHFS  │  ├─ workspace/  │
│  ├─ media/      │ Rsync  │  ├─ media/      │
│  └─ backup/     │        │  └─ backup/     │
└─────────────────┘         └─────────────────┘
```

---

## 三、详细实施步骤

### 步骤1：SSH免密登录配置（双向）

#### 1.1 小龙虾 → Hermes

```bash
# 在小龙虾服务器执行
ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519_hermes -N ""
ssh-copy-id -i ~/.ssh/id_ed25519_hermes.pub user@hermes_ip
```

#### 1.2 Hermes → 小龙虾

```bash
# 在Hermes服务器执行
ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519_xiaolongxia -N ""
ssh-copy-id -i ~/.ssh/id_ed25519_xiaolongxia.pub user@xiaolongxia_ip
```

#### 1.3 配置SSH别名

**小龙虾服务器** (`~/.ssh/config`):
```
Host hermes
    HostName <Hermes内网IP>
    User <用户名>
    IdentityFile ~/.ssh/id_ed25519_hermes
    Port 22
    ServerAliveInterval 60
    ServerAliveCountMax 3
```

**Hermes服务器** (`~/.ssh/config`):
```
Host xiaolongxia
    HostName <小龙虾内网IP>
    User <用户名>
    IdentityFile ~/.ssh/id_ed25519_xiaolongxia
    Port 22
    ServerAliveInterval 60
    ServerAliveCountMax 3
```

---

### 步骤2：安装SSHFS

#### 小龙虾服务器（阿里云Linux 3）
```bash
sudo dnf install -y epel-release
sudo dnf install -y sshfs
```

#### Hermes服务器（根据实际系统）
```bash
# Ubuntu/Debian
sudo apt install -y sshfs

# CentOS/RHEL
sudo yum install -y epel-release
sudo yum install -y sshfs
```

---

### 步骤3：创建共享目录

#### 两台服务器都执行
```bash
# 创建共享目录
sudo mkdir -p /shared/workspace
sudo mkdir -p /shared/media
sudo mkdir -p /shared/backup

# 设置权限
sudo chown -R $USER:$USER /shared
chmod 755 /shared
```

---

### 步骤4：配置SSHFS挂载

#### 小龙虾挂载Hermes
```bash
# 创建挂载点
mkdir -p ~/hermes-shared

# 挂载（临时）
sshfs hermes:/shared ~/hermes-shared -o IdentityFile=~/.ssh/id_ed25519_hermes -o reconnect -o ServerAliveInterval=60 -o allow_other

# 永久挂载（/etc/fstab）
echo "sshfs#hermes:/shared /home/$(whoami)/hermes-shared fuse.sshfs IdentityFile=/home/$(whoami)/.ssh/id_ed25519_hermes,reconnect,ServerAliveInterval=60,allow_other,_netdev 0 0" | sudo tee -a /etc/fstab
```

#### Hermes挂载小龙虾
```bash
# 创建挂载点
mkdir -p ~/xiaolongxia-shared

# 挂载（临时）
sshfs xiaolongxia:/shared ~/xiaolongxia-shared -o IdentityFile=~/.ssh/id_ed25519_xiaolongxia -o reconnect -o ServerAliveInterval=60 -o allow_other

# 永久挂载（/etc/fstab）
echo "sshfs#xiaolongxia:/shared /home/$(whoami)/xiaolongxia-shared fuse.sshfs IdentityFile=/home/$(whoami)/.ssh/id_ed25519_xiaolongxia,reconnect,ServerAliveInterval=60,allow_other,_netdev 0 0" | sudo tee -a /etc/fstab
```

---

### 步骤5：配置Rsync定时同步

#### 5.1 创建同步脚本

**小龙虾服务器** (`/usr/local/bin/sync-to-hermes.sh`):
```bash
#!/bin/bash
# 同步小龙虾 → Hermes

LOG_FILE="/var/log/rsync-to-hermes.log"
DATE=$(date '+%Y-%m-%d %H:%M:%S')

echo "[$DATE] 开始同步..." >> $LOG_FILE

# 同步workspace目录
rsync -avz --progress /shared/workspace/ hermes:/shared/workspace/ >> $LOG_FILE 2>&1

# 同步media目录
rsync -avz --progress /shared/media/ hermes:/shared/media/ >> $LOG_FILE 2>&1

# 同步完成后备份
rsync -avz --delete /shared/backup/ hermes:/shared/backup/ >> $LOG_FILE 2>&1

echo "[$DATE] 同步完成" >> $LOG_FILE
```

**Hermes服务器** (`/usr/local/bin/sync-to-xiaolongxia.sh`):
```bash
#!/bin/bash
# 同步Hermes → 小龙虾

LOG_FILE="/var/log/rsync-to-xiaolongxia.log"
DATE=$(date '+%Y-%m-%d %H:%M:%S')

echo "[$DATE] 开始同步..." >> $LOG_FILE

# 同步workspace目录
rsync -avz --progress /shared/workspace/ xiaolongxia:/shared/workspace/ >> $LOG_FILE 2>&1

# 同步media目录
rsync -avz --progress /shared/media/ xiaolongxia:/shared/media/ >> $LOG_FILE 2>&1

# 同步完成后备份
rsync -avz --delete /shared/backup/ xiaolongxia:/shared/backup/ >> $LOG_FILE 2>&1

echo "[$DATE] 同步完成" >> $LOG_FILE
```

#### 5.2 设置定时任务

**两台服务器都执行**:
```bash
# 编辑crontab
crontab -e

# 添加以下内容（每5分钟同步一次）
*/5 * * * * /usr/local/bin/sync-to-hermes.sh  # 小龙虾执行
*/5 * * * * /usr/local/bin/sync-to-xiaolongxia.sh  # Hermes执行
```

---

## 四、安全配置

### 4.1 阿里云安全组配置

| 方向 | 协议 | 端口 | 源/目标 | 说明 |
|------|------|------|---------|------|
| 入站 | TCP | 22 | 对方服务器IP | SSH连接 |
| 入站 | TCP | 22 | 对方服务器IP | SSH连接 |

### 4.2 SSH安全加固

```bash
# 编辑 /etc/ssh/sshd_config
sudo nano /etc/ssh/sshd_config

# 修改以下配置
Port 22
PermitRootLogin no
PasswordAuthentication no
PubkeyAuthentication yes
AllowUsers <用户名>

# 重启SSH服务
sudo systemctl restart sshd
```

---

## 五、监控与维护

### 5.1 监控脚本

**检查连接状态** (`/usr/local/bin/check-connection.sh`):
```bash
#!/bin/bash
# 检查SSH连接状态

if ssh -o ConnectTimeout=5 -o BatchMode=yes hermes echo "OK" 2>/dev/null; then
    echo "✅ Hermes连接正常"
else
    echo "❌ Hermes连接失败"
    # 发送告警（可选）
    # curl -X POST "https://your-webhook-url" -d "Hermes连接失败"
fi

if ssh -o ConnectTimeout=5 -o BatchMode=yes xiaolongxia echo "OK" 2>/dev/null; then
    echo "✅ 小龙虾连接正常"
else
    echo "❌ 小龙虾连接失败"
fi
```

### 5.2 日志查看

```bash
# 查看同步日志
tail -f /var/log/rsync-to-hermes.log
tail -f /var/log/rsync-to-xiaolongxia.log

# 查看SSH连接日志
journalctl -u sshd -f
```

---

## 六、故障排除

### 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| SSH连接超时 | 网络不通/安全组未配置 | 检查安全组规则，确认内网连通 |
| SSHFS挂载失败 | 权限问题/SSH密钥错误 | 检查密钥权限(600)，确认用户权限 |
| Rsync同步失败 | 磁盘空间不足/权限问题 | 检查磁盘空间，确认目录权限 |
| 定时任务不执行 | crontab配置错误 | 检查crontab格式，查看系统日志 |

### 快速诊断命令

```bash
# 测试SSH连接
ssh hermes echo "连接测试成功"
ssh xiaolongxia echo "连接测试成功"

# 测试SSHFS挂载
ls ~/hermes-shared/
ls ~/xiaolongxia-shared/

# 测试Rsync
rsync -avzn /shared/ hermes:/shared/  # 模拟同步
```

---

## 七、方案对比

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| **SSHFS+Rsync** | 简单、安全、实时+备份 | 需要配置SSH | 推荐方案 |
| NFS | 标准协议、性能好 | 安全性低、配置复杂 | 内网环境 |
| Samba | Windows兼容 | 配置复杂、性能一般 | 跨平台 |
| Syncthing | 双向同步、去中心化 | 需要安装额外服务 | 多节点同步 |
| Rsync only | 简单可靠 | 单向同步、非实时 | 备份场景 |

---

## 八、实施计划

| 阶段 | 任务 | 预计时间 | 负责人 |
|------|------|---------|--------|
| 1 | SSH免密登录配置 | 10分钟 | 诸葛斌 |
| 2 | 安装SSHFS | 5分钟 | 诸葛斌 |
| 3 | 创建共享目录 | 5分钟 | 诸葛斌 |
| 4 | 配置SSHFS挂载 | 10分钟 | 诸葛斌 |
| 5 | 配置Rsync定时同步 | 10分钟 | 诸葛斌 |
| 6 | 安全配置 | 10分钟 | 诸葛斌 |
| 7 | 测试验证 | 15分钟 | 诸葛斌 |
| **总计** | | **约55分钟** | |

---

## 九、后续优化

1. **性能优化**：根据实际使用情况调整Rsync同步频率
2. **监控告警**：接入钉钉/微信告警通知
3. **备份策略**：定期备份到OSS对象存储
4. **扩展性**：如需增加第三台服务器，按相同流程配置

---

**文档版本**：v1.0  
**创建时间**：2026-05-15  
**维护者**：小龙虾 - 诸葛虾  
**最后更新**：2026-05-15