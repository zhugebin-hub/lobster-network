# 大数据与知识图谱应用实验报告

## 学术领域知识图谱构建与查询系统

---

**姓名：** 陈怡  
**日期：** 2026 年 4 月 2 日  
**课程：** 大数据与知识图谱  
**章节：** 第 3 章 大数据与知识图谱应用

---

## 一、实验目的

### 1.1 实验背景

知识图谱（Knowledge Graph）是一种结构化的语义知识库，用于描述现实世界中的实体、概念及其关系。本实验基于第 3 章大数据与知识图谱内容，实现一个学术领域知识图谱应用。

### 1.2 实验目标

| 目标 | 说明 |
|------|------|
| **理解知识图谱概念** | 掌握实体、关系、属性的基本概念 |
| **实现图谱构建** | 使用 Python 实现知识图谱数据结构 |
| **实现查询功能** | 实现多种图谱查询算法 |
| **数据可视化** | 实现图谱的文本可视化展示 |
| **实践应用** | 构建学术领域知识图谱并分析 |

---

## 二、实验环境

### 2.1 软硬件环境

| 项目 | 配置 |
|------|------|
| **操作系统** | Linux 5.10.134-19.2.al8.x86_64 |
| **编程语言** | Python 3 |
| **开发工具** | VS Code / Terminal |
| **依赖库** | 标准库（json, collections, typing） |

### 2.2 实验文件

| 文件 | 说明 |
|------|------|
| `kg_app.py` | 知识图谱应用主程序 |
| `kg_data.json` | 导出的图谱数据文件 |
| `实验报告.md/docx` | 实验报告文档 |

---

## 三、系统设计

### 3.1 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                     用户交互层                          │
│              （命令行界面 / API 接口）                    │
└─────────────────────┬───────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────┐
│                     应用功能层                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐             │
│  │ 实体管理 │  │ 关系管理 │  │ 查询分析 │             │
│  └──────────┘  └──────────┘  └──────────┘             │
└─────────────────────┬───────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────┐
│                     数据存储层                          │
│         （内存数据结构 / JSON 文件持久化）                 │
└─────────────────────────────────────────────────────────┘
```

### 3.2 核心类设计

#### 3.2.1 Entity（实体类）

```python
class Entity:
    """实体类 - 表示知识图谱中的节点"""
    def __init__(self, entity_id: str, name: str, 
                 entity_type: str, properties: dict = None):
        self.entity_id = entity_id      # 实体唯一标识
        self.name = name                 # 实体名称
        self.entity_type = entity_type   # 实体类型
        self.properties = properties     # 实体属性
```

#### 3.2.2 Relation（关系类）

```python
class Relation:
    """关系类 - 表示知识图谱中的边"""
    def __init__(self, head_entity: str, relation_type: str, 
                 tail_entity: str, properties: dict = None):
        self.head_entity = head_entity       # 头实体
        self.relation_type = relation_type   # 关系类型
        self.tail_entity = tail_entity       # 尾实体
        self.properties = properties         # 关系属性
```

#### 3.2.3 KnowledgeGraph（知识图谱类）

```python
class KnowledgeGraph:
    """知识图谱类"""
    def __init__(self):
        self.entities: Dict[str, Entity] = {}           # 实体集合
        self.relations: List[Relation] = []             # 关系集合
        self.entity_by_type: Dict[str, Set[str]] = ...  # 类型索引
        self.relations_by_head: Dict[str, List] = ...   # 出边索引
        self.relations_by_tail: Dict[str, List] = ...   # 入边索引
```

### 3.3 数据模型

**实体类型：**
- 学者（Scholar）
- 大学（University）
- 研究领域（Research Field）
- 论文（Paper）

**关系类型：**
- 任职于（Works At）
- 毕业于（Graduated From）
- 研究领域（Research Field）
- 发表论文（Published Paper）
- 包含子领域（Contains Subfield）
- 应用于（Applied To）

---

## 四、核心功能实现

### 4.1 知识图谱构建

```python
def build_academic_kg() -> KnowledgeGraph:
    """构建学术领域知识图谱"""
    kg = KnowledgeGraph()
    
    # 添加学者实体
    scholars = [
        Entity("S001", "吴恩达", "学者", {"国籍": "美国", "职位": "教授"}),
        Entity("S002", "李飞飞", "学者", {"国籍": "美国", "职位": "教授"}),
        Entity("S003", "Yann LeCun", "学者", {"国籍": "法国", "职位": "首席 AI 科学家"}),
        # ... 更多学者
    ]
    
    # 添加大学、研究领域、论文实体
    # ...
    
    # 添加关系
    relations = [
        Relation("S001", "任职于", "U001", {"时间": "2002-至今"}),
        Relation("S001", "研究领域", "F001", {}),
        # ... 更多关系
    ]
    
    return kg
```

### 4.2 查询功能实现

#### 4.2.1 按类型查询

```python
def query_by_type(self, entity_type: str) -> List[Entity]:
    """按类型查询实体"""
    return [self.entities[eid] for eid in 
            self.entity_by_type.get(entity_type, set())]
```

#### 4.2.2 邻居查询

```python
def get_neighbors(self, entity_id: str, direction: str = 'out') -> List[Relation]:
    """获取邻居节点（关系）"""
    if direction == 'out':
        return self.relations_by_head.get(entity_id, [])
    elif direction == 'in':
        return self.relations_by_tail.get(entity_id, [])
```

#### 4.2.3 关系路径查询（BFS 算法）

```python
def query_relation_path(self, start_entity: str, end_entity: str, 
                        max_depth: int = 3) -> List[List[str]]:
    """查询两个实体之间的关系路径（BFS）"""
    from collections import deque
    
    queue = deque([(start_entity, [start_entity])])
    visited = {start_entity}
    paths = []
    
    while queue and len(paths) < 5:
        current, path = queue.popleft()
        
        if current == end_entity:
            paths.append(path)
            continue
        
        if len(path) > max_depth:
            continue
        
        for relation in self.relations_by_head.get(current, []):
            next_entity = relation.tail_entity
            if next_entity not in visited:
                visited.add(next_entity)
                queue.append((next_entity, path + [next_entity]))
    
    return paths
```

### 4.3 可视化实现

```python
def generate_text_visualization(kg: KnowledgeGraph) -> str:
    """生成文本可视化"""
    lines = []
    
    # 按类型分组显示
    for entity_type, entity_ids in kg.entity_by_type.items():
        lines.append(f"\n【{entity_type}】")
        for eid in entity_ids:
            entity = kg.get_entity(eid)
            lines.append(f"  ○ {entity.name}")
            
            # 显示该实体的出边
            for rel in kg.relations_by_head.get(eid, []):
                tail = kg.get_entity(rel.tail_entity)
                lines.append(f"    └─[{rel.relation_type}]→ {tail.name}")
    
    return "\n".join(lines)
```

---

## 五、实验结果

### 5.1 知识图谱构建结果

```
============================================================
正在构建学术领域知识图谱...
============================================================

✓ 添加实体：吴恩达 (学者)
✓ 添加实体：李飞飞 (学者)
✓ 添加实体：Yann LeCun (学者)
✓ 添加实体：Geoffrey Hinton (学者)
✓ 添加实体：周志华 (学者)
✓ 添加实体：马毅 (学者)
✓ 添加实体：斯坦福大学 (大学)
✓ 添加实体：加州大学伯克利分校 (大学)
✓ 添加实体：纽约大学 (大学)
✓ 添加实体：多伦多大学 (大学)
✓ 添加实体：南京大学 (大学)
✓ 添加实体：深度学习 (研究领域)
✓ 添加实体：计算机视觉 (研究领域)
✓ 添加实体：自然语言处理 (研究领域)
✓ 添加实体：强化学习 (研究领域)
✓ 添加实体：机器学习 (研究领域)
✓ 添加实体：ImageNet Classification... (论文)
✓ 添加实体：Deep Learning (论文)
✓ 添加实体：Attention Is All You Need (论文)
```

### 5.2 查询功能演示

#### 查询 1：按类型查询学者

```
【查询 1】查询所有学者：
----------------------------------------
  • 马毅 (美国)
  • Yann LeCun (法国)
  • 周志华 (中国)
  • Geoffrey Hinton (英国)
  • 李飞飞 (美国)
  • 吴恩达 (美国)
```

#### 查询 2：查询实体邻居

```
【查询 2】吴恩达的相关关系：
----------------------------------------
  • 任职于 → 斯坦福大学
  • 研究领域 → 深度学习
  • 研究领域 → 强化学习
```

#### 查询 3：关系路径查询

```
【查询 3】吴恩达 到 计算机视觉 的关系路径：
----------------------------------------
  路径 1: 吴恩达 → 深度学习 → 计算机视觉
```

#### 查询 4：图谱统计信息

```
【查询 4】知识图谱统计信息：
----------------------------------------
  • 实体总数：19
  • 关系总数：23
  • 实体类型分布：{'学者': 6, '大学': 5, '研究领域': 5, '论文': 3}
  • 平均关系数：1.21
```

#### 查询 5：共同研究领域分析

```
【查询 5】研究深度学习的学者：
----------------------------------------
  • 吴恩达 (教授)
  • 李飞飞 (教授)
  • Yann LeCun (首席 AI 科学家)
  • Geoffrey Hinton (教授)
  • 周志华 (教授)
```

### 5.3 图谱可视化

```
【学者】
  ○ 马毅
    └─[任职于]→ 加州大学伯克利分校
    └─[研究领域]→ 计算机视觉
  ○ Yann LeCun
    └─[任职于]→ 纽约大学
    └─[研究领域]→ 深度学习
    └─[研究领域]→ 计算机视觉
  ○ 周志华
    └─[任职于]→ 南京大学
    └─[研究领域]→ 机器学习
    └─[研究领域]→ 深度学习
  ...

【研究领域】
  ○ 机器学习
    └─[包含子领域]→ 深度学习
  ○ 深度学习
    └─[应用于]→ 计算机视觉
    └─[应用于]→ 自然语言处理
```

### 5.4 数据导出

```
✓ 知识图谱已导出到：/home/admin/.openclaw/workspace/kg_data.json
```

---

## 六、实验分析

### 6.1 性能分析

| 操作 | 时间复杂度 | 空间复杂度 |
|------|-----------|-----------|
| 添加实体 | O(1) | O(n) |
| 添加关系 | O(1) | O(m) |
| 按类型查询 | O(k) | O(1) |
| 邻居查询 | O(k) | O(1) |
| 路径查询 | O(V+E) | O(V) |

> 注：n=实体数，m=关系数，k=结果数，V=顶点数，E=边数

### 6.2 功能评估

| 功能 | 实现状态 | 评估 |
|------|---------|------|
| 实体管理 | ✅ 完成 | 支持增删改查 |
| 关系管理 | ✅ 完成 | 支持多种关系类型 |
| 类型查询 | ✅ 完成 | 高效索引查询 |
| 路径查询 | ✅ 完成 | BFS 算法实现 |
| 数据导出 | ✅ 完成 | JSON 格式持久化 |
| 可视化 | ✅ 完成 | 文本形式展示 |

### 6.3 知识图谱特点

| 特点 | 说明 |
|------|------|
| **结构化** | 实体 - 关系 - 实体三元组 |
| **语义化** | 关系具有明确语义 |
| **可扩展** | 支持动态添加实体和关系 |
| **可查询** | 支持多种查询模式 |

---

## 七、心得体会

### 7.1 技术收获

1. **理解了知识图谱的核心概念**
   - 实体是节点，关系是边
   - 属性描述实体和关系的特征
   - 索引结构对查询性能至关重要

2. **掌握了图谱构建方法**
   - 定义本体（实体类型、关系类型）
   - 抽取实体和关系
   - 建立索引加速查询

3. **实践了图算法**
   - BFS 用于路径查询
   - 邻居遍历用于关系分析
   - 类型索引用于分类查询

### 7.2 应用思考

知识图谱在实际中的应用非常广泛：

| 领域 | 应用场景 |
|------|---------|
| **搜索引擎** | 语义搜索、知识卡片 |
| **推荐系统** | 基于知识的推荐 |
| **智能问答** | 问题理解、答案检索 |
| **数据分析** | 关联分析、模式发现 |

### 7.3 不足与改进

| 不足 | 改进方向 |
|------|---------|
| 数据存储简单 | 使用图数据库（Neo4j） |
| 可视化基础 | 使用 Graphviz/D3.js |
| 缺少推理能力 | 添加规则推理引擎 |
| 数据量小 | 接入真实数据集 |

---

## 八、实验总结

### 8.1 实验成果

- ✅ 实现了完整的知识图谱数据结构
- ✅ 构建了包含 19 个实体、23 个关系的学术图谱
- ✅ 实现了 5 种查询功能并验证正确性
- ✅ 完成了数据持久化和可视化展示

### 8.2 核心代码

核心代码文件：`kg_app.py`（约 300 行）

主要模块：
1. 数据结构定义（Entity, Relation）
2. 知识图谱类（KnowledgeGraph）
3. 图谱构建函数（build_academic_kg）
4. 查询演示函数（demo_queries）
5. 可视化函数（generate_text_visualization）

### 8.3 知识要点

| 知识点 | 掌握程度 |
|--------|---------|
| 知识图谱概念 | ⭐⭐⭐⭐⭐ |
| 图数据结构 | ⭐⭐⭐⭐⭐ |
| 图查询算法 | ⭐⭐⭐⭐ |
| 数据可视化 | ⭐⭐⭐⭐ |
| Python 编程 | ⭐⭐⭐⭐⭐ |

---

## 九、参考资料

1. 刘峤等。知识图谱构建方法综述。计算机学报，2016.
2. 赵军。知识图谱。科学出版社，2019.
3. Neo4j Documentation. https://neo4j.com/docs/
4. Google Knowledge Graph. https://developers.google.com/knowledge-graph
5. 第 3 章 大数据与知识图谱课程教材

---

## 附录

### 附录 A：运行截图

```
（此处插入程序运行截图）
- 知识图谱构建过程
- 查询功能演示
- 可视化输出
```

### 附录 B：数据文件

**kg_data.json** - 导出的知识图谱数据

```json
{
  "entities": [
    {"entity_id": "S001", "name": "吴恩达", "entity_type": "学者", ...},
    ...
  ],
  "relations": [
    {"head_entity": "S001", "relation_type": "任职于", "tail_entity": "U001", ...},
    ...
  ]
}
```

### 附录 C：完整代码

详见 `kg_app.py` 文件。

---

**报告完成时间：** 2026 年 4 月 2 日  
**实验时长：** 约 1.5 小时  
**代码行数：** 约 300 行
