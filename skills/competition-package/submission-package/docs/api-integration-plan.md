# 🦞 会议室预约虾 - 浙江工商大学 API 接入方案

**学校**: 浙江工商大学（ZJGSU）
**校区**: 下沙校区
**接入系统**: 会议室/自习室预约系统（宜搭平台）
**接入时间**: 2026 年 4 月初（下周）
**负责人**: CJY 陈俊烨

---

## 📋 接入背景

### 当前状态
- **项目名称**: 会议室预约虾
- **比赛**: 环球黑客松｜OPC 极限挑战赛（上海站）
- **赛道**: 赛道二 | AI 合伙人
- **当前数据源**: 本地 JSON 文件（模拟数据）
- **目标数据源**: 浙江工商大学宜搭预约系统 API

### 接入目标
```
┌─────────────────────────────────────────────────────┐
│              API 接入目标                            │
├─────────────────────────────────────────────────────┤
│                                                     │
│  1. 实现真实预约功能                                │
│  2. 支持浙江工商大学下沙校区                        │
│  3. 覆盖会议室、自习室、实验室等场景                │
│  4. 与学校现有系统无缝集成                          │
│  5. 保证数据安全和用户隐私                          │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 🏗️ 技术架构

### 当前架构
```
┌─────────────────────────────────────────────────────┐
│                    用户交互层                        │
│         钉钉/微信/网页对话框 (自然语言输入)            │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│                  AI 理解层 (NLU)                      │
│   时间解析 | 人数解析 | 类型解析 | 设备需求           │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│                  智能匹配层                          │
│   容量过滤 | 类型过滤 | 时间匹配 | 推荐排序           │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│                  执行层（当前）                      │
│   预约提交：写入 bookings.json                      │
│   冲突检测：查询 bookings.json                      │
│   通知反馈：返回预约号、房间信息、时间               │
└─────────────────────────────────────────────────────┘
```

### 接入后架构
```
┌─────────────────────────────────────────────────────┐
│                    用户交互层                        │
│         钉钉/微信/网页对话框 (自然语言输入)            │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│                  AI 理解层 (NLU)                      │
│   时间解析 | 人数解析 | 类型解析 | 设备需求           │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│                  智能匹配层                          │
│   容量过滤 | 类型过滤 | 时间匹配 | 推荐排序           │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│                  执行层（接入后）                    │
│   预约提交：调用宜搭 API → 写入学校系统              │
│   冲突检测：调用宜搭 API → 查询占用状态              │
│   通知反馈：返回预约号、房间信息、时间               │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│              浙江工商大学宜搭平台                    │
│   会议室预约系统 | 自习室预约系统 | 实验室管理系统   │
└─────────────────────────────────────────────────────┘
```

---

## 📡 API 对接方案

### 需要获取的 API 信息

#### 1. 认证相关
- [ ] API 认证方式（OAuth2 / API Key / JWT）
- [ ] 认证流程
- [ ] Token 有效期
- [ ] 刷新机制

#### 2. 房间查询 API
- [ ] 获取所有会议室列表
- [ ] 获取房间详细信息（容量、设备、位置）
- [ ] 查询房间可用时间段
- [ ] 过滤条件（容量、类型、设备、楼栋）

#### 3. 预约操作 API
- [ ] 创建预约
- [ ] 查询预约记录
- [ ] 取消预约
- [ ] 修改预约
- [ ] 冲突检测

#### 4. 用户相关 API
- [ ] 用户身份验证
- [ ] 用户权限查询（学生/教师/管理员）
- [ ] 用户预约历史

---

## 🔧 代码适配方案

### 当前数据层
```javascript
// data/meeting-rooms-xiasha.json
{
  "campus": "下沙校区",
  "rooms": [
    {
      "id": "XD-101",
      "name": "信电报告厅 101",
      "building": "信电大楼",
      "capacity": 80,
      "type": "报告厅",
      "availableSlots": [...]
    }
  ]
}
```

### 适配后数据层
```javascript
// api/yida-adapter.js
class YidaAdapter {
  constructor(config) {
    this.apiKey = config.apiKey;
    this.baseUrl = config.baseUrl;
  }

  // 获取会议室列表
  async getRooms(filters = {}) {
    const response = await fetch(`${this.baseUrl}/rooms`, {
      headers: { 'Authorization': `Bearer ${this.apiKey}` }
    });
    return await response.json();
  }

  // 查询可用时间段
  async getAvailableSlots(roomId, date) {
    const response = await fetch(
      `${this.baseUrl}/rooms/${roomId}/slots?date=${date}`,
      { headers: { 'Authorization': `Bearer ${this.apiKey}` } }
    );
    return await response.json();
  }

  // 创建预约
  async createBooking(booking) {
    const response = await fetch(`${this.baseUrl}/bookings`, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${this.apiKey}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify(booking)
    });
    return await response.json();
  }

  // 查询预约记录
  async getBookings(userId) {
    const response = await fetch(
      `${this.baseUrl}/bookings?userId=${userId}`,
      { headers: { 'Authorization': `Bearer ${this.apiKey}` } }
    );
    return await response.json();
  }
}
```

### 适配层集成
```javascript
// scripts/book-meeting-room.js (修改后)
const fs = require('fs');
const path = require('path');

// 数据源配置
const DATA_SOURCE = process.env.DATA_SOURCE || 'json'; // 'json' | 'yida'

let dataAdapter;

if (DATA_SOURCE === 'yida') {
  const YidaAdapter = require('../api/yida-adapter');
  dataAdapter = new YidaAdapter({
    apiKey: process.env.YIDA_API_KEY,
    baseUrl: process.env.YIDA_API_URL
  });
} else {
  const JsonAdapter = require('../api/json-adapter');
  dataAdapter = new JsonAdapter();
}

// 主函数中使用
async function main(query) {
  const parsed = parseRequest(query);
  const candidates = await dataAdapter.findAvailableRooms(parsed);
  const booking = await dataAdapter.createBooking(best);
  // ...
}
```

---

## 📅 接入时间线

### 第 1 周（2026.03.31 - 2026.04.06）

| 日期 | 任务 | 状态 |
|------|------|------|
| 03.31 | 与学校信息中心初次对接 | 🟡 待进行 |
| 04.01 | 获取 API 文档和测试环境 | 🟡 待进行 |
| 04.02 | 测试 API 连通性 | 🟡 待进行 |
| 04.03 | 数据格式分析 | 🟡 待进行 |
| 04.04 | 编写适配层代码 | 🟡 待进行 |
| 04.05 | 单元测试 | 🟡 待进行 |
| 04.06 | 集成测试 | 🟡 待进行 |

### 第 2 周（2026.04.07 - 2026.04.13）

| 日期 | 任务 | 状态 |
|------|------|------|
| 04.07 | 用户认证集成 | 🟡 待进行 |
| 04.08 | 真实数据测试 | 🟡 待进行 |
| 04.09 | 试点运行（信电学院） | 🟡 待进行 |
| 04.10 | 收集用户反馈 | 🟡 待进行 |
| 04.11 | Bug 修复 | 🟡 待进行 |
| 04.12 | 性能优化 | 🟡 待进行 |
| 04.13 | 正式上线 | 🟡 待进行 |

---

## 🔒 安全与隐私

### 数据安全
- [ ] API Key 加密存储（环境变量）
- [ ] HTTPS 传输
- [ ] 敏感信息脱敏
- [ ] 访问日志记录

### 用户隐私
- [ ] 学号/工号加密
- [ ] 预约记录权限控制
- [ ] 数据保留期限
- [ ] 用户数据删除机制

### 合规要求
- [ ] 符合学校信息安全规定
- [ ] 符合个人信息保护法
- [ ] 数据使用授权

---

## 📊 测试计划

### 测试环境
```
环境：开发环境（测试 API）
数据：测试会议室数据
用户：测试账号
```

### 测试用例

#### 功能测试
- [ ] 查询会议室列表
- [ ] 查询可用时间段
- [ ] 创建预约
- [ ] 查询预约记录
- [ ] 取消预约
- [ ] 冲突检测

#### 性能测试
- [ ] API 响应时间 < 500ms
- [ ] 并发支持 > 100 QPS
- [ ] 数据一致性

#### 安全测试
- [ ] 认证失效处理
- [ ] 权限验证
- [ ] SQL 注入防护
- [ ] XSS 防护

---

## 📞 联系方式

### 学校对接
- **部门**: 浙江工商大学信息中心
- **联系人**: [待填写]
- **邮箱**: [待填写]
- **电话**: [待填写]

### 项目团队
- **负责人**: CJY 陈俊烨
- **邮箱**: [待填写]
- **电话**: [待填写]

---

## ✅ 接入检查清单

### 前期准备
- [ ] 与学校信息中心建立联系
- [ ] 获取 API 文档
- [ ] 申请测试环境账号
- [ ] 了解数据格式规范

### 开发阶段
- [ ] 编写适配层代码
- [ ] 实现认证模块
- [ ] 实现数据查询模块
- [ ] 实现预约操作模块
- [ ] 编写单元测试

### 测试阶段
- [ ] API 连通性测试
- [ ] 功能测试
- [ ] 性能测试
- [ ] 安全测试
- [ ] 用户验收测试

### 上线阶段
- [ ] 生产环境配置
- [ ] 数据迁移（如需要）
- [ ] 试点运行
- [ ] 正式上线
- [ ] 监控告警

---

## 🎯 预期效果

### 功能提升
```
接入前:
• 模拟数据（22 间会议室）
• 本地存储（JSON 文件）
• 无法真实预约

接入后:
• 真实数据（全校会议室）
• 学校系统集成
• 可真实预约
```

### 用户体验
```
接入前:
• 演示性质
• 数据不实时
• 无法实际使用

接入后:
• 生产可用
• 数据实时同步
• 实际解决痛点
```

### 比赛加分
```
• 真实场景落地 ✅
• 与学校系统对接 ✅
• 可规模化推广 ✅
• 商业价值明确 ✅
```

---

*API 接入方案版本：v1.0*
*创建日期：2026-03-28*
*负责人：CJY 陈俊烨*
*团队：陈俊烨 + AI 助手 信电大虾 🦞⚡️*

**🎯 下周接入，比赛加分！**
