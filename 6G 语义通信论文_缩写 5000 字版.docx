面向 6G 语义通信的深度学习边缘智能体知识演化与自适应传输机制

诸葛斌，洪仕玉，许云汉，董黎刚，张子天，蒋献
（浙江工商大学信息与电子工程学院（萨塞克斯人工智能学院），浙江 杭州 310018）

摘  要：面向 6G 语义通信中分布式协同面临的高昂信令开销与环境非平稳性挑战，本文提出一种面向深层语义智能体的知识演化与自适应传输机制。首先，将边缘协同过程建模为以感知节点为主体的部分可观察马尔可夫决策过程（POMDP），并引入局部敏感哈希（LSH）算子将高维语义特征投影至低维流形，构建轻量级动态知识对齐字典。其次，提出知识增强型近端策略优化（KE-PPO）算法，利用信息瓶颈（IB）理论重构联合奖励函数，并引入基于策略熵驱动的动态融合权重机制。基于真实视觉数据集与时变衰落信道的实验表明，智能体仅凭局部观测即可自主输出环境适配的多级语义传输策略，显著降低了传统分布式方案中繁重的全局交互负担。实验结果证实，该机制显著改善了传统纯强化学习在非平稳环境中的策略震荡问题，在极端信道下展现出优异的鲁棒性。

关键词：6G 语义通信；自演化知识库；近端策略优化（PPO）；自适应协同传输

1  引言

随着移动通信技术从 5G 向 6G 演进，未来通信网络正经历着从单纯追求比特可靠传输向注重语义有效传递与理解的深刻变革。6G 愿景下的万物智联、全息通信和数字孪生等新兴应用场景，不仅催生了海量的多模态数据，更对通信系统的智能化、低时延与高能效提出了前所未有的严苛要求。然而，基于香农信息论建立的传统通信系统在面对 6G 时代的海量数据暴增时，已逐渐显露出带宽瓶颈，难以满足未来网络对超可靠低时延（URLLC）的极致追求。

语义通信（Semantic Communication）作为一种突破香农范式瓶颈的新兴架构，旨在仅提取并传输与下游任务高度相关的核心语义信息，从而大幅滤除冗余数据，并在极低信噪比（SNR）等恶劣信道环境下表现出卓越的鲁棒性。现有语义通信的研究多局限于单点传输或静态编解码优化，并通常假定收发双方始终具备全局共享且静态同构的"先验知识库"。然而，在实际的 6G 分布式边缘计算网络中，边缘设备因物理位置、观测视角与算力的差异，智能体之间的知识状态存在客观的异构性。当环境发生突变时，这种"知识失配"将导致严重的语义歧义与通信效能下降。

在复杂的边缘协同推理场景中，为应对动态时变信道与资源分配挑战，近年来部分前沿研究尝试引入多智能体强化学习（MARL）进行联合通信决策。然而，在真实的 6G 物联网场景中，边缘节点往往面临严苛的电量与带宽约束，要求多个节点之间高频交互全局信道状态信息（CSI）与策略参数以维持 MARL 的收敛，将产生庞大的交互开销。此外，多智能体在处理高维动态环境时容易面临"环境非平稳性"与维度灾难等难题，导致算法震荡且难以实时部署。

鉴于此，本文提出一种面向深层语义智能体的知识演化与自适应传输机制。本文将通信交互过程建模为以边缘感知节点为主体的部分可观察马尔可夫决策过程（POMDP），利用基于哈希索引的动态经验字典表征语义知识库，并提出知识增强的近端策略优化（KE-PPO）算法进行协同策略求解。本文的主要贡献如下：

（1）提出了融合 LSH 语义流形的边缘协同通信框架。针对多智能体协同对全局信息依赖度高的问题，构建了单智能体 POMDP 模型。引入局部敏感哈希（LSH）映射机制确立了高维特征向低维流形投影的误差界限，在有效降低决策空间维度的同时，构建了轻量级的动态知识字典。

（2）设计了基于信息瓶颈（IB）理论的知识演化机制。引入动态演化范式，将通信传输代价与推理意图互信息之间的博弈重构为 IB 约束下的联合寻优问题，驱动经验字典随物理信道状态时序更新。

（3）提出了基于策略熵驱动的动态融合传输机制（KE-PPO）。构建了由策略熵量化决策不确定性的自适应融合机制，将深度强化学习的全局探索特征与演化知识库的先验经验进行数学耦合，有效缓解了高动态非平稳环境下静态超参数设定引发的动作选择崩溃风险。

（4）构建了结合真实多模态数据集与时变信道的评估矩阵。仿真实验表明，所提机制在极低信令开销下，有效逼近了全局完美感知下的帕累托最优前沿；在极端拥塞的衰落信道中，系统通过主动降级展现出优异的鲁棒性。

2  系统模型与问题建模

2.1  分布式边缘协同语义通信系统模型

本文考虑一个面向 6G 智联网（AIoT）的分布式边缘协同通信系统。系统由多个配备有限算力与射频模块的无线边缘节点（如智能视觉传感器、无人机或边缘网关）组成。在传统的通信范式下，海量的原始感知数据直接通过无线信道传输，极易在带宽受限或深度衰落的环境下引发极高的传输时延与系统拥塞。

为了突破这一物理瓶颈，本文以典型的"分布式协同视觉推理任务"为例，构建了端边协同语义通信系统。在该模型中，系统节点被划分为两大核心实体：

发送智能体 A（边缘感知节点）：部署于数据生成的源头，负责在边缘端采集高维的原始视觉信号，并利用内置的语义算子执行"语义特征提取"。智能体 A 将剔除视觉冗余信息，抽取出低维的核心语义特征，并将其转化为适合无线传输的比特流。仿真中采用轻量级 ResNet-18 作为语义算子以适配边缘算力约束。

接收智能体 B（边缘汇聚节点）：通常部署于算力充沛的边缘汇聚服务器（MEC）端，负责接收受损的语义流，进行纠错、融合，并完成最终的下游意图推理（如目标检测、场景分类）。

2.2  语义知识库（SKB）与状态矩阵演化

在实际的边缘协同场景中，由于收发双方物理位置的差异、环境观测视角的局部性以及无线信道的时变性，发送端与接收端之间往往存在不可避免的语义理解偏差。为缓解这种"知识非同步"导致的推理失效，本文为深层语义智能体引入了演化型语义知识库（Semantic Knowledge Base, SKB）。在数学表征上，我们将 SKB 显式定义为一个映射状态 - 动作经验价值的动态记忆字典 K(s, a)。

本机制中的"自演化"与传统边缘缓存或经验回放存在本质区别。传统缓存通常依赖静态的键值匹配来存储原始特征或固定信道状态，难以应对 6G 高动态环境下的特征分布漂移。而本文的 SKB 本质上是一个随环境非平稳性动态更新的概率演化流形。在每一轮通信决策后，知识库的状态更新方程定义为：

K(s_t, a_t)_{t+1} = K(s_t, a_t)_t + η·(R_t - K(s_t, a_t)_t)

此更新在信息瓶颈约束下，对高维语义状态在低维子空间上的期望效用进行后验概率近似。通过引入衰减因子η，SKB 能够有效平滑高频时变信道带来的瞬态噪声，使隐层特征分布与接收端的真实意图需求随时间演进实现动态对齐。

2.3  协同传输策略的边缘单智能体 POMDP 重构

在真实的分布式网络中，边缘感知节点受限于物理距离与信令开销，无法获取全局网络拓扑或其他接收节点的真实内在状态，只能依靠本地侦测进行不完全感知。因此，本文摒弃了极易引发维度爆炸与非平稳性的多智能体联合建模，将该协同传输策略优化问题重构为以边缘感知节点为主体的单智能体部分可观察马尔可夫决策过程（POMDP）。

该边缘决策 POMDP 由元组⟨S, A, P, R, Ω, O⟩刻画：
- S：全局真实状态空间，包含整个网络的全局动态 CSI 与所有节点的真实内部状态矩阵。
- A：联合动作空间，即各个感知节点实时选择的语义特征降级与压缩级别。
- P：状态转移概率，受外界物理层动态衰落信道变化的随机影响，同时遵循内部知识库演化规律。
- R：联合奖励函数，基于信息瓶颈（IB）理论，定义为意图互信息最大化与通信压缩开销最小化的帕累托折中目标函数。
- Ω与 O：局部观察空间与观察函数。感知节点 i 在 t 时刻的局部观察 o_i(t) 包含其侦测到的物理信道信噪比（SNR），以及表征收发双方知识异构性的失配度（KL 散度代理特征）。

3  基于 KE-PPO 的边缘协同传输优化算法

3.1  基于知识库增强的 PPO 协同传输架构

在分布式边缘协同语义通信系统中，由于物理信道的时变衰落特性，多智能体联合优化通常面临极高的全局信道状态信息（CSI）交互开销。传统的边缘多智能体强化学习在处理此类问题时，极易陷入环境非平稳性与维数灾难陷阱。为此，本文将协同传输策略优化问题重构为以边缘感知节点为主体的独立单智能体 POMDP，并提出知识增强型近端策略优化（KE-PPO）算法，通过引入动态语义知识库（SKB），在规避高昂节点间信令开销的同时，有效弥补了单智能体局部观测的局限性。

3.2  联合状态特征提取与知识库降维映射

在本文的实验实现中，边缘智能体 i 的局部观测向量 o_i(t) 被定义为一个 529 维的联合连续特征向量：前 512 维为前端算力提取的高维语义特征；中间 16 维为当前物理信噪比（SNR）特征；最后 1 维为衡量收发双方知识异构性的 KL 散度指标。

考虑到 6G 视觉语义特征高度聚集在一个低维语义流形上，本文提出一种基于局部敏感哈希（LSH）的语义子空间投影机制。

定理 1（基于局部敏感哈希的语义流形误差界定）：给定边缘视觉联合特征集合 X ∈ R^N（N=529），若存在基于随机正交映射的哈希降维算子 f: R^N → R^k（k=8），对于任意两个在原空间中极近的特征状态向量 u, v ∈ X，其在降维特征空间中的语义欧氏距离能够以概率 1-δ满足：
(1-ε)||u-v||² ≤ ||f(u)-f(v)||² ≤ (1+ε)||u-v||²

我们在保证ε< 0.15 的容忍度下，通过语义子空间投影构建了高维状态的一阶近似代理索引。即使大幅降维至 k=8，原空间中具有高度意图相似性的样本被哈希到同一索引桶的概率将呈指数级逼近 1，将知识库的搜索空间复杂度降低了数个数量级。

3.3  共享底层特征的 Actor-Critic 与知识融合机制

基于 529 维的联合状态特征，本文构建了共享底层双头网络结构，让策略头与价值头共享前置的特征抽象层。

输入与共享隐藏层：网络输入层接收 529 维联合状态向量。隐藏层采用两层各 256 个神经元的全连接网络，使用 ReLU 激活函数。

Actor 策略头：输出维度为 3 的逻辑值，经 Softmax 转化为概率分布，代表智能体选择无损特征（Level 0）、适度压缩特征（Level 1）以及极限压缩特征（Level 2）的决策置信度。

Critic 价值头：输出标量值 V(S)，评估当前本地状态的预期折扣回报。

本文算法的创新在于将神经网络输出与知识库经验进行深度耦合。设策略网络输出的原始动作概率分布为 P_ppo，从知识库 K_i(t) 中提取的经验 Q 值经 Softmax 归一化得到 P_kb。最终动作概率分布为：
P_final = (1 - α_t)·P_ppo + α_t·P_kb

动态融合权重α_t 由策略熵 H(P_ppo) 与信噪比 f_snr 联合驱动：
α_t = clip(γ·H(P_ppo) - η·f_snr, α_min, α_max)

当信道环境剧烈突变或知识严重失配时，策略熵激增，系统自动拔高α_t，强化对知识库先验经验的依赖；在稳态优质信道下，α_t 自动衰减，释放深度强化学习的探索潜力。

3.4  联合优化奖励函数设计

本文引入信息瓶颈（IB）理论重构联合优化奖励函数：
R = max(I(Z;Y) - β·I(X;Z))

其中，I(X;Z) 表征压缩开销，β为拉格朗日乘子，I(Z;Y) 表征语义特征与下游意图的互信息量。

采用变分互信息下界近似：I(Z;Y) ≥ E[log q_θ(Y|Z)] + H(Y)。本文利用 BERT-Score 作为经验代理，最大化 BERT-Score 即等价于最大化互信息下界。

3.5  智能体训练与知识同步更新

协同训练闭环包含四个核心步骤：

（1）环境交互与知识库在线演化：每个训练回合交互 100 步，每一步将状态转移元组存入经验回放池，同时触发知识库更新。

（2）广义优势函数评估（GAE）：采用 GAE 方法，设定γ = 0.99，λ = 0.95。

（3）信任域裁剪更新：采用 Mini-batch=64，Epochs=10，裁剪参数ε=0.2。

（4）综合损失函数：L_total = -L^{CLIP} + 0.5·L^{VF} - 0.05·S[π_θ]。

4  实验设计与数据分析

4.1  实验场景与数据集

实验选用 MS COCO 2017 Validation Set，筛选 1000 张涵盖典型 6G 边缘物联网场景的图像。构建三级递进式语义表示：
- Level 0：完整上下文描述，保真度极高。
- Level 1：核心主谓宾三元组，适度折中。
- Level 2：极简核心实体标签，保障基础感知。

4.2  物理层参数

通信信道：Sub-6GHz/毫米波宽带传输，带宽 100 MHz，SNR 波动范围 -10 dB 至 20 dB。

部署范式：云端/MEC 离线集中训练，边缘在线分布式执行。感知端节点（15 TOPS 算力）仅需执行轻量级网络前向推理与 LSH 哈希字典查表更新，算力开销处于亚毫秒级。

4.3  实验结果分析

4.3.1  算法收敛性与系统能效对比

对比算法：Greedy（全量传输）、Random（随机）、Pure PPO（无知识库）、Static-KB（静态权重）、KE-PPO（本文）。

实验结果表明，KE-PPO 奖励值稳定在 44.4，远超 Pure PPO（33.6）和 Greedy（33.3）。引入 SKB 知识库后，系统收益实现约 32% 的阶跃式增长。在系统能效方面，KE-PPO 录得 0.0045，相比 Greedy 和 Pure PPO（均约 0.0024）实现近 2 倍的能效提升。

4.3.2  极端信道鲁棒性与消融分析

消融实验验证了核心模块的有效性：
（1）知识库引导作用：Pure PPO 在全频段内准确率均出现明显衰退，印证了 SKB 对提升决策稳定性的积极作用。
（2）动态熵驱动机制：Static-KB（α=0.3）在复杂信道下表现欠佳，而本文的动态融合机制能够在"信任先验"与"自主探索"之间达成动态平衡。

4.3.3  知识库演化动力学

知识库状态覆盖率呈现对数型收敛趋势。前 200 回合处于高频探索阶段，400 回合后趋于稳定，形成约 15000 个独立状态的饱和平台，验证了 LSH 降维算子的保距性与收敛有界性。

4.3.4  熵驱动微观动力学

α_t 在 0.44 至 0.58 之间波动。当环境剧烈衰落时，策略熵激增，α_t 自适应拉升，智能体依赖知识库先验经验实施风险规避；在优质稳态期，α_t 自动衰减，释放探索潜力。

5  结束语

本文针对 6G 语义通信中环境波动与边缘知识失配挑战，提出了知识演化与自适应传输机制。引入 LSH 算子确立特征降维误差界限，将通信决策重构为 POMDP，提出融合 IB 理论与熵驱动动态融合权重的 KE-PPO 算法。实验验证了所提机制的理论有效性与综合性能优势，为未来 6G 边缘智联网络中轻量级、意图驱动的多节点协同提供了理论参考与可行范式。

参考文献：
[1] STRINATI E C, et al. 6G networks: Beyond Shannon towards semantic and goal-oriented communications[J]. Computer Networks, 2021, 190: 107930.
[2] 张平，等。语义通信：6G 智能化网络的新范式 [J]. 移动通信，2021, 45(6): 1-12.
[3] ZHANG P, et al. Toward wisdom-evolutionary and primitive-concise 6G[J]. Engineering, 2022, 8: 60-73.
[4] XIE H, et al. Deep learning enabled semantic communication systems[J]. IEEE TSP, 2021, 69: 2663-2675.
[5] YANG W, et al. Semantic communications for future internet[J]. IEEE COMST, 2023, 25(1): 213-250.
