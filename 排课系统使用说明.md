# 🎯 一键启动包使用说明

## 📦 文件说明

您已收到：**scheduling-system.zip** (约 30KB)

这是完整的排课系统 Web 版，包含所有必要文件。

---

## 🖥️ Windows 用户 - 超简单 3 步

### 第 1 步：解压文件
- 右键点击 `scheduling-system.zip`
- 选择"解压到当前文件夹"或"Extract Here"
- 得到 `scheduling-system` 文件夹

### 第 2 步：安装 Python（如已安装可跳过）
1. 访问 https://www.python.org/downloads/
2. 下载 Python 3.8 或更高版本
3. **重要**：安装时勾选 ✅ "Add Python to PATH"

### 第 3 步：一键启动
1. 打开 `scheduling-system` 文件夹
2. **双击运行** `start.bat`
3. 等待自动安装依赖（首次运行）
4. 浏览器打开：**http://localhost:5000**

---

## 🍎 Mac 用户 - 简单 3 步

### 第 1 步：解压文件
- 双击 `scheduling-system.zip` 自动解压

### 第 2 步：打开终端
- 打开"终端"应用
- 输入 `cd ` (注意空格)
- 把 `scheduling-system` 文件夹拖入终端窗口
- 按回车

### 第 3 步：一键启动
```bash
chmod +x start.sh
./start.sh
```
浏览器打开：**http://localhost:5000**

---

## 🐧 Linux 用户

```bash
unzip scheduling-system.zip
cd scheduling-system
chmod +x start.sh
./start.sh
```
浏览器打开：**http://localhost:5000**

---

## ✅ 成功标志

看到以下提示表示成功：
```
========================================
     正在启动排课系统...
========================================

📱 浏览器访问地址:
   http://localhost:5000

⚠️  请勿关闭此窗口！
   按 Ctrl+C 可停止服务
```

---

## 📋 使用流程

1. 打开浏览器访问 http://localhost:5000
2. 点击"教师管理" → 添加教师
3. 点击"班级管理" → 添加班级
4. 点击"课程设置" → 设置课程
5. 点击"排课系统" → 开始排课
6. 选择班级查看课表

---

## 💾 数据保存

- 所有数据保存在 `data/school.db`
- 备份此文件即可保存所有数据
- 下次启动时数据自动加载

---

## 🔧 常见问题

**Q: 双击 start.bat 没反应？**
- 确保已安装 Python
- 确保安装时勾选了"Add Python to PATH"
- 重新安装 Python 试试

**Q: 提示端口被占用？**
- 关闭其他占用 5000 端口的程序
- 或修改 app.py 中的端口号

**Q: 如何停止服务？**
- 关闭黑色命令窗口即可
- 或按 Ctrl+C

**Q: 如何备份数据？**
- 复制 `data/school.db` 文件到安全位置

---

## 📞 需要帮助？

如有任何问题，请联系技术支持。

祝您使用愉快！🎉
