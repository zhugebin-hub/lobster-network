# 基于时空扩散图注意力网络的交通流量预测方法

**中文题名**：基于时空扩散图注意力网络的交通流量预测方法

**英文题名**：Traffic Flow Forecasting Method Based on Spatio-Temporal Diffusion Graph Attention Network

---

**摘要**：针对交通流量预测中存在的动态空间依赖刻画不足、多尺度时间模式建模不充分以及长时预测误差易累积等问题，提出一种基于时空扩散图注意力网络的交通流量预测方法。该方法构建编码器—解码器总体框架，在时间维度引入多尺度时间编码模块，通过扩张因果卷积提取短期波动、阶段性趋势与潜在周期模式；在空间维度设计时间感知动态图模块，使节点间关联权重能够随当前时间语义自适应变化；进一步结合图扩散注意力机制，对静态拓扑先验、潜在全局依赖与有向扩散传播过程进行统一建模。为验证所提方法的有效性，在 METR-LA、PEMS-BAY 和 NE-BJ 三个公开交通数据集上开展对比实验，并与 HA、VAR、FNN、STGCN、DCRNN、Graph WaveNet、GMAN、MTGNN、DGCRN 及 RGDAN 等方法进行比较。实验结果表明，所提方法在两个高速公路数据集和一个复杂城市路网数据集上均取得了优于对比方法的预测性能。其中，在 Horizon 12 任务下，METR-LA、PEMS-BAY 和 NE-BJ 数据集上的 MAE 分别达到 3.02、1.56 和 4.25，MAPE 分别达到 8.33%、3.82% 和 15.53%。消融实验进一步表明，多尺度时间编码、时间感知动态图和图扩散注意力三类模块对性能提升均具有显著贡献。研究结果说明，所提方法能够有效提高复杂交通场景下的中长期预测精度，为智能交通系统中的交通态势研判与运行调度提供技术支撑。

**关键词**：交通流量预测；时空图神经网络；动态图学习；扩散注意力；智能交通

---

## 1 引言

随着智慧城市、车联网和城市感知基础设施的持续发展，交通系统正源源不断地产生高维、异构、强关联的时空数据。交通流量预测是智能交通系统中的基础性任务，其结果可直接服务于拥堵预警、信号控制、路径诱导与运行调度等应用场景。由于交通状态同时受到道路拓扑、区域功能、人群出行行为和突发事件等多重因素影响，交通流量序列通常表现出显著的非线性、多尺度时间依赖和动态空间耦合特征 [1]。因此，如何在统一模型框架下有效刻画时间动态与空间传播规律，仍然是交通预测研究中的核心问题。

传统交通流量预测方法主要依赖统计学习模型，如历史平均法、ARIMA 或 VAR 等，这类方法具有实现简单、解释性较强的优点，但往往建立在线性关系或弱平稳性假设之上，难以适应复杂路网中的强非平稳交通序列 [3]。随着深度学习的发展，LSTM、GRU、TCN 和 Transformer 等模型被逐步应用于交通预测任务，在时间依赖建模方面取得了明显进展 [4]。然而，若仅从时间维度对交通数据进行建模，则难以充分反映节点之间的拓扑关联和交通状态在空间上的传导过程。

图神经网络为交通预测提供了更适合的建模范式。STGCN、DCRNN、Graph WaveNet、GMAN、MTGNN 和 DGCRN 等方法通过图卷积、扩散卷积、自适应图学习和注意力机制等技术，有效增强了交通网络中的时空依赖建模能力 [6,8,10]。尽管如此，现有方法仍存在两方面不足。其一，不少方法仍主要依赖静态邻接矩阵或弱动态图结构，难以充分反映不同时间语义下的动态空间依赖关系。其二，时间建模与空间建模往往相对独立，尤其在中长期预测任务中，对短期波动、周期模式和空间传播机制的协同刻画仍显不足，导致误差随预测步长增长而明显累积 [11]。

针对上述问题，本文提出一种基于时空扩散图注意力网络的交通流量预测方法。该方法在整体上采用编码器—解码器结构，在时间维度通过多尺度时间编码模块学习不同时间跨度的依赖关系，在空间维度通过时间感知动态图模块实现节点关系的动态调整，并结合图扩散注意力机制对静态拓扑先验、潜在全局依赖和有向传播规律进行统一建模。与已有方法相比，本文工作的特点在于：一方面，模型将时间特征提取与动态图结构生成紧密耦合，使空间关系能够随时间语义动态变化；另一方面，通过扩散注意力机制同时建模局部邻域传播与潜在非局部关联，在保证表达能力的同时兼顾计算效率。

本文以实验验证为主线展开论证。在 METR-LA、PEMS-BAY 和 NE-BJ 三个公开交通数据集上的实验结果表明，所提方法在不同预测跨度和不同交通场景下均表现出更好的预测精度与鲁棒性，尤其在长期预测任务中优势更为明显。消融实验和效率分析进一步证明了各关键模块设计的合理性和整体框架的实用价值。

---

## 2 模型方法

### 2.1 总体框架

本文提出的时空扩散图注意力网络命名为 ST-DGATNet。模型整体采用编码器—解码器架构，由时空嵌入层、时空预测块和输出映射层组成。给定历史时间窗口内的交通状态序列后，模型首先构建节点空间嵌入和时间嵌入，并将两者融合为统一的时空表示。随后，在编码端利用多尺度时间编码模块提取时间动态特征，在此基础上由时间感知动态图模块生成与当前时间窗口相关的动态图结构，再通过图扩散注意力机制完成空间传播建模。最后，解码端在未来时间嵌入条件下对编码特征进行重构，输出未来多个时间步的预测结果。

这种结构的核心思想在于，不再将时间建模和空间建模简单串联，而是通过"时间特征驱动动态图生成—动态图反向增强时空表征"的方式形成协同建模框架。对于交通流量预测而言，时间维度中的高峰、平峰和周期语义会直接影响节点之间的实际关联强度，因此让图结构随时间语义动态变化具有明显合理性。

### 2.2 时空嵌入表示

为统一建模路网拓扑信息与时间位置信息，本文在输入阶段设计时空嵌入（Spatio-Temporal Embedding, STE）生成机制。该模块由空间嵌入生成单元与时间嵌入生成单元两部分组成 [63]。

在空间建模方面，首先依据道路网络中节点之间的相对距离构建基础邻接矩阵，并将其作为图结构输入至空间特征学习模块。设道路网络包含 *N* 个节点，记第 *i* 个节点为 *vᵢ*。采用 Node2vec 方法对图结构进行表示学习，捕获节点间的潜在拓扑关系及高阶邻域信息。得到的节点表示进一步通过两层全连接网络映射至 *D* 维嵌入空间，生成空间嵌入表示 **e**ᵢ^S ∈ ℝ^D^。

在时间建模方面，为刻画交通数据中的周期性规律与时间位置信息，将每个时间步对应的星期信息与时刻信息进行 one-hot 编码，得到 ℝ^7^ 与 ℝ^T^ 表示，其中 7 表示一周的天数，*T* 表示一天被划分的时间片数量。二者拼接后形成 ℝ^T+7^ 的时间特征向量，随后输入多层感知机（MLP），映射得到时间嵌入 **e**ᵀ ∈ ℝ^D^。

在预测框架中，时间序列被划分为 *P* 个历史时间步与 *Q* 个未来预测时间步。历史时间嵌入表示为 **e**ᵢ^T ∈ ℝ^D^，其中 *tᵢ* ∈ {*t₁, t₂, ..., t_P*}；未来时间嵌入表示为 **e**ⱼ^T ∈ ℝ^D^，其中 *tⱼ* ∈ {*t_{P+1}, t_{P+2}, ..., t_{P+Q}*}。

采用广播机制将空间嵌入与对应时间嵌入进行逐元素相加，从而构造历史时空嵌入张量 **E**^H^ ∈ ℝ^P×N×D^ 以及未来时空嵌入张量 **E**^F^ ∈ ℝ^Q×N×D^。在第 *l* 个编码器层中，将历史时空嵌入 **E**^H^ 与上一层输出 **H**^(l−1)^ 在特征维度上进行拼接：

$$\mathbf{H}^{(l)} = f\left(\text{Concat}(\mathbf{H}^{(l-1)}, \mathbf{E}^H)\right) \tag{1}$$

其中，**H**^(l)^ 表示第 *l* 层编码器的输出，Concat(·) 表示特征维度上的拼接操作，*f*(·) 表示由线性变换和激活函数组成的非线性映射函数。

同理，在第 *l* 个解码器层中，将未来时空嵌入 **E**^F^ 与中间表示 **H**^(L)^ 进行拼接：

$$\mathbf{H}^{(l+L+1)} = f\left(\text{Concat}(\mathbf{H}^{(L)}, \mathbf{E}^F)\right) \tag{2}$$

其中，*L* 表示编码器层数，**H**^(l+L+1)^ 为对应解码层的输出表示。

### 2.3 多尺度时间编码模块

交通流量序列通常同时包含短时波动、阶段性趋势与周期性模式。为提升模型在中长期预测中的时间建模能力，本文设计多尺度时间编码（Multi-scale Temporal Encoding, MTE）模块。该模块首先对原始序列进行线性映射，将低维交通特征投影到高维隐空间；随后利用多层扩张因果卷积沿时间维度提取不同感受野下的依赖关系，并通过残差连接保证深层特征传播的稳定性。

设交通网络中共有 *N* 个节点，历史时间窗口长度为 *T*，批大小为 *B*。每个节点在时间 *t* 的交通流量为单变量特征，即 *F* = 1。历史时间窗口内的输入序列表示为 **X** ∈ ℝ^B×T×N×F^。

MTE 首先对输入序列进行两层线性特征映射，将其投影至高维隐空间：

$$\mathbf{X}^{(0)} = \text{GELU}\left(\mathbf{X}\mathbf{W}_1 + \mathbf{b}_1\right)\mathbf{W}_2 + \mathbf{b}_2 \tag{3}$$

其中，**W**₁ ∈ ℝ^F×D^，**W**₂ ∈ ℝ^D×D^，**b**₁, **b**₂ ∈ ℝ^D^，*D* 为隐空间特征维度，GELU(·) 为非线性激活函数。经过映射后得到 **X**^(0)^ ∈ ℝ^B×T×N×D^。

在获得初始隐空间表示 **X**^(0)^ 后，MTE 采用多层扩张因果卷积对时间维度进行建模。设共有 *L* 层时间卷积，第 *l* 层的扩张率为 *d_l*，卷积核大小为 *k*。第 *l* 层在时间步 *t* 的输出表示为：

$$\text{Conv}_l\left(\mathbf{X}^{(l-1)}\right)_t = \sum_{i=0}^{k-1} \mathbf{\Theta}_l(i) \cdot \mathbf{X}^{(l-1)}_{t-d_l \cdot i} \tag{4}$$

其中，Θ_l(i) ∈ ℝ^D×D^ 为可学习权重矩阵。

为增强深层特征传播能力并缓解梯度消失问题，在每一层扩张卷积后引入残差连接：

$$\mathbf{X}^{(l)} = \mathbf{X}^{(l-1)} + \text{Conv}_l\left(\mathbf{X}^{(l-1)}\right) \tag{5}$$

该卷积结构严格满足时间因果约束，即任意时刻的输出仅依赖当前及历史观测信息。当扩张率集合为 {*d₁, d₂, ..., d_L*} 时，第 *L* 层的时间感受野为：

$$R = 1 + (k-1)\sum_{l=1}^{L} d_l \tag{6}$$

若扩张率按指数方式增长，例如 {1, 2, 4}，则感受野随层数呈近似指数增长，从而在较少层数下建模较长时间跨度的依赖关系。

扩张因果卷积具有两方面优势。其一，它严格遵循时间因果约束，保证预测阶段只依赖当前及历史观测信息。其二，通过扩张率递增的卷积结构，可以在较少层数下快速扩大感受野，从而同时兼顾局部扰动和长时依赖。与递归结构相比，该模块更易实现并行计算；与单尺度卷积相比，该模块对长周期模式的建模能力更强。对于交通流量预测任务而言，多尺度时间编码模块能够有效缓解长期预测中由于局部建模不足导致的误差积累问题。

### 2.4 时间感知动态图模块

在真实交通网络中，节点之间的空间依赖并非始终固定不变，而会随着时间语义、出行需求强度和交通状态变化而动态调整。例如，在早晚高峰阶段，部分关键枢纽节点的影响力会显著增强，其交通状态对其他节点的传播作用明显强于平峰时段。为此，本文提出时间感知动态图（Temporal-Aware Graph, TAG）模块，通过时间增强特征提取当前时间窗口下的节点表示，并基于节点重要性得分构建动态图邻接矩阵 [79]。

设经过时间卷积模块处理后的时空特征表示为 **X** ∈ ℝ^B×T×N×D^。为获得当前时间窗口下的全局节点语义表示，沿时间维与批次维进行均值聚合：

$$\mathbf{H} = \frac{1}{B \times T} \sum_{b=1}^{B}\sum_{t=1}^{T} \mathbf{X}_{b,t,:,:} \tag{7}$$

得到 **H** ∈ ℝ^N×D^，其中第 *i* 行 **h**ᵢ ∈ ℝ^D^ 表示节点 *i* 在当前时间语义下的综合特征表示。这种双重聚合具有两方面意义：其一，时间维聚合提取时间窗口内的整体趋势信息；其二，批次维聚合提高统计稳定性，减弱样本随机波动的影响。

随后通过线性映射与非线性变换增强节点表征能力：

$$\tilde{\mathbf{H}} = \text{GELU}\left(\mathbf{H}\mathbf{W}_p + \mathbf{b}_p\right) \tag{8}$$

其中 **W**_p ∈ ℝ^D×D^ 为可学习参数矩阵。

为构建时间相关的动态图结构，引入节点重要性打分机制。通过线性映射将每个节点压缩为标量权重：

$$\mathbf{s} = \tilde{\mathbf{H}}\mathbf{w}_s \tag{9}$$

其中 **w**_s ∈ ℝ^D×1^ 为可学习参数，**s** ∈ ℝ^N×1^ 表示节点重要性向量，*sᵢ* 表示节点 *i* 在当前时间语义下的影响强度。

基于节点重要性向量构造节点两两之间的关联强度矩阵：

$$\mathbf{A} = \mathbf{s}\mathbf{s}^T \tag{10}$$

其元素形式为 *Aᵢⱼ* = *sᵢsⱼ*。可以看出，矩阵 **A** 为秩一结构，即 rank(**A**) = 1。该结构表明动态图由一个全局主方向所决定，节点间关联强度由各自重要性共同调制。当两个节点在当前时间语义下均具有较高影响力时，其连接强度自然增强；若某节点重要性较低，则其整体空间影响力相应减弱。

随后对矩阵 **A** 进行行归一化处理：

$$\mathbf{A}^{tag}_{ij} = \frac{\mathbf{A}_{ij}}{\sum_{k=1}^{N}\mathbf{A}_{ik}} \tag{11}$$

得到时间感知动态邻接矩阵 **A**^tag^ ∈ ℝ^N×N^。由式 (11) 可知，**A**^tag^ 满足 **A**^tag^ **1** = **1**，即每一行元素之和为 1，因此该矩阵可解释为概率转移矩阵，有助于后续图传播过程的数值稳定性。

为兼顾长期稳定空间结构与短期时间驱动结构，将动态邻接矩阵与静态自适应邻接矩阵进行加权融合：

$$\mathbf{A}_f = \alpha \mathbf{A}^{static} + (1-\alpha)\mathbf{A}^{tag} \tag{12}$$

其中 **A**^static^ 为可训练的静态邻接矩阵，α ∈ (0, 1) 为可学习融合系数。

TAG 模块的主要计算复杂度包括：节点得分计算 *O(ND)*、外积构图 *O(N²)*、行归一化 *O(N²)*。相比于基于节点对拼接的注意力方法通常需要 *O(N²D)* 的复杂度，TAG 在保持表达能力的同时显著降低计算开销，更适用于大规模交通网络场景。

### 2.5 图扩散注意力机制

在生成动态空间结构后，本文进一步构建图扩散注意力机制，以增强对交通状态传播过程的刻画能力。该机制由随机图注意力、自适应邻接传播以及双向扩散融合三部分组成。

在图结构数据建模中，传统图注意力网络通常通过显式计算节点间的交互相似度生成注意力权重，其时间复杂度与节点规模呈平方关系增长。为降低计算复杂度并提升可扩展性，本文采用随机图注意力机制。该机制的核心思想在于：注意力权重不通过节点间显式交互计算生成，而是通过参数化随机矩阵直接构造，并结合图结构掩码进行约束。

具体而言，在第 *tᵢ* 个时间步，首先生成随机矩阵 **R**^ti^，随后通过掩码函数保留图结构中存在连接关系的元素：

$$\mathbf{A}^{t_i} = \text{softmax}\left(\text{mask}(\mathbf{R}^{t_i})\right) \tag{13}$$

其中，mask(·) 用于约束注意力权重仅作用于原图中存在连接的节点对。对于节点 *vᵢ* 与其邻居节点 *vⱼ*，注意力权重表示为：

$$e_{v_i,v_j} = \begin{cases} \exp(w_{v_i,v_j}) & \text{if } v_j \in \mathcal{N}_i \\ 0 & \text{otherwise} \end{cases} \tag{14}$$

其中，𝒩ᵢ 表示节点 *vᵢ* 的邻居集合，*w_{vᵢ,vⱼ}* 为可学习参数。节点特征更新方式定义为：

$$\mathbf{H}^R_{t_i} = \sigma\left(\mathbf{A}^{t_i} f(\mathbf{H}_{t_i})\right) \tag{15}$$

其中 *f*(·) 为非线性变换函数。与传统 GAT 相比，该机制不依赖节点间的显式相似度交互，而是通过训练过程直接学习一组适用于任务的权重分布。由于避免了逐对节点交互计算，其注意力权重矩阵的生成复杂度可近似视为常数级 *O(1)*，从而显著降低计算开销。

为增强模型表达能力与训练稳定性，引入多头机制。设注意力头数为 *K*，则多头随机注意力表示为：

$$\mathbf{H}^R_{t_i} = \Big\Vert_{k=1}^{K} \sigma\left(\mathbf{A}^{k,t_i} f_k\left(\mathbf{H}^{k}_{t_i}\right)\right) \tag{16}$$

其中 ∥ 表示拼接操作，*f_k*(·) 表示第 *k* 个子空间的非线性变换，输出维度为 *D/K*。

尽管随机图注意力能够实现邻域权重自适应分配，但其仍依赖于显式图结构。为进一步挖掘潜在空间依赖关系，本文引入自适应邻接矩阵：

$$\mathbf{A}^{adp} = \text{softmax}\left(\text{ReLU}(\mathbf{E}_1\mathbf{E}_2^T)\right) \tag{17}$$

$${\mathbf{A}^{adp}}^T = \text{softmax}\left(\text{ReLU}(\mathbf{E}_2\mathbf{E}_1^T)\right) \tag{18}$$

其中 **E**₁, **E**₂ ∈ ℝ^N×C^ 为可学习嵌入矩阵。**A**^adp^ 表示交通流的正向传播权重，(**A**^adp^)^T^ 表示反向传播权重。通过该机制，模型能够在预定义拓扑结构之外，自适应学习潜在节点关联强度。

为统一建模随机注意力与自适应邻接传播过程，构建图扩散注意力模块。考虑到道路网络通常可表示为非对称有向图，进一步构建双向扩散机制 [63]：

$$\mathbf{H}^{s(l)} = \Theta\left(\mathbf{H}^{(l)}, \mathbf{A}^{adp}, \mathbf{A}^R\right) + \Theta^T\left(\mathbf{H}^{(l)}, {\mathbf{A}^{adp}}^T, {\mathbf{A}^R}^T\right) \tag{19}$$

其中，Θ(·) 表示前向传播过程，对应交通流入方向的信息扩散；Θ^T^(·) 表示反向传播过程，对应交通流出方向的信息传播。通过双向扩散机制，模型能够更加充分刻画交通网络中的方向性依赖关系。

综上所述，图扩散注意力机制通过随机注意力建模、自适应邻接学习以及双向扩散传播的协同作用，在保证计算效率的前提下，有效增强了模型对多阶、动态、非均匀空间依赖关系的表达能力，为后续时间建模模块提供更加稳定且具有判别性的空间特征表示。

---

## 3 实验设计

### 3.1 数据集与预处理

为了验证模型在不同交通场景下的适用性，本文选取三个具有代表性的公开交通数据集开展实验。METR-LA 数据集包含洛杉矶高速公路 207 个传感器采集的交通速度数据；PEMS-BAY 数据集包含加州湾区 325 个传感器记录的交通速度数据；NE-BJ 数据集来源于真实城市地面交通网络，包含 500 个路段的速度观测序列。三个数据集分别覆盖了高速公路与复杂城市路网两类典型场景，能够较好检验模型对不同空间结构和流量波动特征的适应能力。

| 数据集 | 节点数 | 时间粒度 | 任务类型 | 场景特点 |
|--------|--------|----------|----------|----------|
| METR-LA | 207 | 5 min | 交通速度预测 | 高速公路场景，周期性与拥堵波动并存 |
| PEMS-BAY | 325 | 5 min | 交通速度预测 | 规模较大，长期趋势相对稳定 |
| NE-BJ | 500 | 5 min | 交通速度预测 | 城市路网场景，非平稳性与复杂扰动更强 |

在数据预处理方面，本文采用与经典交通预测研究一致的数据划分方式，即将前 70% 样本用作训练集，中间 10% 用作验证集，后 20% 用作测试集，以保证实验结果的可比性 [7]。同时，使用 Z-score 标准化方法对各数据集进行归一化处理。基础邻接矩阵由节点之间的距离信息构建，定义如下：

$$A_{ij} = \begin{cases} \exp\left(-\dfrac{d_{ij}^2}{\sigma^2}\right) & \text{if } \exp\left(-\dfrac{d_{ij}^2}{\sigma^2}\right) \geq \xi \\ 0 & \text{otherwise} \end{cases} \tag{20}$$

其中，*dᵢⱼ* 表示节点 *vᵢ* 到 *vⱼ* 的距离，ξ = 0.1 为稀疏性阈值，σ 为距离标准差。

### 3.2 对比方法与评价指标

为了全面验证所提方法的性能，本文从统计学习、基础深度学习和时空图神经网络三个层面选取对比模型。统计学习方法包括历史平均法 HA 和向量自回归模型 VAR；基础深度学习方法包括前馈神经网络 FNN；时空预测方法包括 STGCN、DCRNN、Graph WaveNet、GMAN、MTGNN、DGCRN 和 RGDAN。上述模型覆盖了基于静态图、自适应图、动态图、扩散卷积和注意力建模等多类代表性技术路线，能够较全面地反映所提方法的相对优势。

| 方法类别 | 模型 | 主要特征 |
|----------|------|----------|
| 统计学习 | HA、VAR | 实现简单，但对复杂非线性和时空耦合关系建模能力有限 |
| 基础深度学习 | FNN | 能拟合部分非线性关系，但不显式利用图结构 |
| 时空图神经网络 | STGCN、DCRNN、Graph WaveNet、GMAN、MTGNN、DGCRN、RGDAN | 分别代表静态图、扩散建模、自适应图、时空注意力和动态图学习等主流路线 |

本文采用 MAE、RMSE 和 MAPE 三项指标评价模型性能，定义如下：

$$\text{MAE} = \frac{1}{N}\sum_{i=1}^{N} |y_i - \hat{y}_i| \tag{21}$$

$$\text{RMSE} = \sqrt{\frac{1}{N}\sum_{i=1}^{N}(y_i - \hat{y}_i)^2} \tag{22}$$

$$\text{MAPE} = \frac{100\%}{N}\sum_{i=1}^{N}\left|\frac{y_i - \hat{y}_i}{y_i}\right| \tag{23}$$

其中，*yᵢ*、ŷᵢ 分别表示真实值和预测值，*N* 表示预测窗口长度，设置为 *N* = 12。实验分别在 Horizon 3、Horizon 6 和 Horizon 12 三个预测跨度下开展。

### 3.3 实现说明

为保证实验设置公平一致，所有模型均在相同的数据划分条件和评价标准下进行训练与测试。所提模型在总体架构上采用统一的编码器—解码器范式，以便在不同数据集之间保持可迁移的实验流程。训练过程中利用验证集选择模型参数，并在测试集上报告最终结果。由于本文的核心目标是验证模型在中长期交通预测任务中的优势，因此分析重点放在 Horizon 12 结果以及复杂城市路网场景下的鲁棒性表现。

---

## 4 实验结果与分析

### 4.1 高速公路场景下的整体预测性能

表 1 给出了 ST-DGATNet 与各类对比方法在 METR-LA 和 PEMS-BAY 数据集上的预测结果。从整体上看，所提模型在 Horizon 3、Horizon 6 和 Horizon 12 三个预测跨度下均取得了最优结果，表明该方法在短期和长期预测任务中都具有稳定优势。

| 数据集 | 模型 | H3 MAE | H3 RMSE | H3 MAPE | H6 MAE | H6 RMSE | H6 MAPE | H12 MAE | H12 RMSE | H12 MAPE |
|--------|------|--------|---------|---------|--------|---------|---------|---------|----------|----------|
| METR-LA | HA | 4.16 | 7.80 | 13.00% | 4.16 | 7.80 | 13.00% | 4.16 | 7.80 | 13.00% |
| | VAR | 4.42 | 7.89 | 10.20% | 5.41 | 9.13 | 12.70% | 6.52 | 10.11 | 15.80% |
| | STGCN | 2.88 | 5.74 | 7.62% | 3.47 | 7.24 | 9.57% | 4.59 | 9.40 | 12.70% |
| | DCRNN | 2.77 | 5.38 | 7.30% | 3.15 | 6.45 | 8.80% | 3.60 | 7.60 | 10.50% |
| | Graph WaveNet | 2.69 | 5.15 | 6.90% | 3.07 | 6.22 | 8.37% | 3.53 | 7.37 | 10.01% |
| | GMAN | 2.80 | 5.55 | 7.41% | 3.12 | 6.49 | 8.73% | 3.44 | 7.35 | 10.07% |
| | MTGNN | 2.69 | 5.18 | 6.86% | 3.05 | 6.17 | 8.19% | 3.49 | 7.23 | 9.87% |
| | DGCRN | 2.62 | 5.01 | 6.63% | 2.99 | 6.05 | 8.19% | 3.44 | 7.19 | 9.73% |
| | RGDAN | 2.69 | 5.20 | 7.14% | 2.96 | 5.98 | 8.07% | 3.36 | 7.02 | 9.54% |
| | **ST-DGATNet** | **2.41** | **4.57** | **6.49%** | **2.63** | **5.37** | **7.11%** | **3.02** | **6.11** | **8.33%** |
| PEMS-BAY | HA | 2.88 | 5.59 | 6.80% | 2.88 | 5.59 | 6.80% | 2.88 | 5.59 | 6.80% |
| | VAR | 1.74 | 3.16 | 3.60% | 2.32 | 4.25 | 5.00% | 2.93 | 5.44 | 6.50% |
| | STGCN | 1.36 | 2.96 | 2.90% | 1.81 | 4.27 | 4.17% | 2.49 | 5.69 | 5.79% |
| | DCRNN | 1.38 | 2.95 | 2.90% | 1.74 | 3.97 | 3.90% | 2.07 | 4.74 | 4.90% |
| | Graph WaveNet | 1.30 | 2.74 | 2.73% | 1.63 | 3.70 | 3.67% | 1.95 | 4.52 | 4.63% |
| | GMAN | 1.34 | 2.91 | 2.86% | 1.63 | 3.76 | 3.68% | 1.86 | 4.32 | 4.37% |
| | MTGNN | 1.32 | 2.79 | 2.77% | 1.65 | 3.74 | 3.69% | 1.94 | 4.49 | 4.53% |
| | DGCRN | 1.28 | 2.69 | 2.66% | 1.59 | 3.63 | 3.55% | 1.89 | 4.42 | 4.43% |
| | RGDAN | 1.31 | 2.79 | 2.77% | 1.56 | 3.55 | 3.47% | 1.82 | 4.20 | 4.28% |
| | **ST-DGATNet** | **1.18** | **2.37** | **2.48%** | **1.53** | **3.20** | **3.20%** | **1.56** | **3.55** | **3.82%** |

从 METR-LA 结果看，所提模型在 Horizon 12 下的 MAE、RMSE 和 MAPE 分别为 3.02、6.11 和 8.33%，相较于表现较强的 RGDAN 分别下降约 10.12%、12.96% 和 12.68%。这一结果说明，在交通状态波动更为剧烈的高速公路场景中，引入时间感知动态图和图扩散注意力能够更有效地提升长时预测精度。尤其在 RMSE 指标上的明显下降，反映出模型对较大误差样本和局部突发波动具有更好的控制能力。

PEMS-BAY 数据集上的结果进一步验证了这一点。在 Horizon 12 下，所提模型的 MAE 为 1.56，较 RGDAN 的 1.82 下降约 14.29%；RMSE 和 MAPE 也分别下降约 15.47% 和 10.75%。由于 PEMS-BAY 数据集节点数量更多、网络规模更大，模型仍能保持优势，表明其对中大规模交通网络具有较好的可扩展性。特别值得注意的是，所提模型在短期预测和长期预测任务中均保持领先，这意味着其优势并非仅来自对某一特定时间尺度的拟合，而是体现在更完整的时空建模能力上。

进一步观察各方法随预测步长增加的误差变化趋势可以发现，所有模型的误差均会随着 Horizon 的增大而上升，这是交通预测任务中普遍存在的不确定性累积现象。然而，ST-DGATNet 的误差增长幅度明显小于其他方法。例如，在 PEMS-BAY 数据集上，STGCN 的 MAPE 从 2.90% 增长至 5.79%，而 ST-DGATNet 仅从 2.48% 增长至 3.82%。这说明所提模型在面对长时依赖时具有更强的稳定性，其原因在于多尺度时间编码模块能够更充分地保留长期趋势信息，而动态图扩散机制则能够持续补充远距离空间传播特征，减弱预测步长扩展造成的信息衰减。

### 4.2 复杂城市路网场景下的泛化能力分析

为了进一步评估模型在复杂城市交通环境中的适用性，本文在 NE-BJ 数据集上开展实验。与高速公路场景相比，城市地面交通网络更容易受到信号灯控制、交叉口排队、出行需求波动和突发事件等多重因素影响，因此其交通状态通常表现出更强的非平稳性与更复杂的空间耦合关系。表 2 给出了各模型在 NE-BJ 数据集上的预测结果。

| 数据集 | 模型 | H3 MAE | H3 RMSE | H3 MAPE | H6 MAE | H6 RMSE | H6 MAPE | H12 MAE | H12 RMSE | H12 MAPE |
|--------|------|--------|---------|---------|--------|---------|---------|---------|----------|----------|
| NE-BJ | HA | 6.00 | 10.95 | 26.40% | 6.00 | 10.95 | 26.40% | 6.00 | 10.95 | 26.40% |
| | VAR | 5.42 | 8.16 | 19.28% | 5.76 | 9.07 | 21.53% | 6.14 | 9.65 | 23.33% |
| | FNN | 4.08 | 7.22 | 13.31% | 5.14 | 9.27 | 18.24% | 6.47 | 11.35 | 25.57% |
| | DCRNN | 3.84 | 6.84 | 12.82% | 4.51 | 8.49 | 15.84% | 5.15 | 9.77 | 19.08% |
| | Graph WaveNet | 3.74 | 6.54 | 12.49% | 4.41 | 8.08 | 15.79% | 4.99 | 9.20 | 19.45% |
| | DGCRN | 3.56 | 6.27 | 12.01% | 4.23 | 7.96 | 15.10% | 4.79 | 9.23 | 17.98% |
| | RGDAN | 3.76 | 6.78 | 12.49% | 4.26 | 7.94 | 14.85% | 4.68 | 8.86 | 17.04% |
| | **ST-DGATNet** | **3.31** | **6.08** | **10.79%** | **4.14** | **7.14** | **13.00%** | **4.25** | **7.80** | **15.53%** |

从结果可以看出，ST-DGATNet 在 NE-BJ 数据集上的所有预测跨度下都取得了最低的 MAPE，且在 MAE 和 RMSE 上同样保持最优。这说明所提方法不仅适用于结构相对规则的高速公路场景，也能够较好地适应复杂城市路网的动态变化。在最具挑战性的 Horizon 12 任务中，所提模型的 MAPE 为 15.53%，较 Graph WaveNet 的 19.45% 下降约 20.15%，较 RGDAN 的 17.04% 下降约 8.86%。这一结果表明，时间感知动态图模块对复杂城市交通中的时变空间依赖具有更强的建模能力。

需要指出的是，复杂城市路网中的交通状态往往包含更多突发峰值与局部异常波动，因此仅观察单一 RMSE 指标可能无法全面反映模型质量。一些方法通过输出更平滑的结果，可能在个别情况下获得较低平方误差，但这并不意味着其对真实交通态势具有更高的刻画能力。相比之下，ST-DGATNet 在 MAPE 上的持续领先说明其在整体相对误差控制方面更具优势，因而更适合应用于实际交通管理中的趋势预警和状态感知任务。

### 4.3 深入对比分析与结果解读

从不同类型对比方法的整体表现来看，统计学习方法 HA 与 VAR 在三个数据集上的性能均明显落后，说明复杂交通流量序列已难以通过简单历史规律或线性回归关系充分刻画。FNN 虽然能够学习一定的非线性映射，但由于缺乏显式空间结构建模能力，其在复杂路网场景下表现依旧有限。这表明交通流量预测的关键不只是时间序列拟合，更在于如何将时间演化和网络结构传播规律统一建模。

进一步比较时空图神经网络方法可以发现，STGCN 和 DCRNN 分别代表了静态图卷积和扩散卷积的经典思路，它们在基于距离构图的场景下表现稳定，但仍依赖固定拓扑结构，难以有效反映不同时间窗口中节点关系的动态变化。Graph WaveNet 和 MTGNN 引入了自适应图学习，较好缓解了固定拓扑的局限，但其动态图更新仍偏重全局静态可学习参数，缺乏对当前时间语义的直接响应。DGCRN 和 RGDAN 则进一步强化了动态图或注意力机制的作用，在多个数据集上已经表现出较强竞争力。ST-DGATNet 在此基础上继续取得提升，说明仅有动态图学习仍不足以完全解决问题，还需要让动态图与时间语义、多尺度时间建模以及扩散传播机制形成更紧密的协同。

就机制层面而言，所提方法的优势主要来源于三点。首先，多尺度时间编码模块提高了模型对不同时间跨度依赖关系的感知能力，使模型能够同时捕捉局部短时扰动、阶段性变化和周期模式，从而在长期预测中保持更高稳定性。其次，时间感知动态图模块使节点关系能够随时间窗口动态变化，更符合真实交通系统中高峰和平峰差异显著的运行机制。最后，图扩散注意力机制不仅强调了关键邻域传播路径，还补充了静态距离图难以表达的潜在非局部空间依赖，因此能够在复杂交通传播场景下提供更强的判别性空间表示。

### 4.4 消融实验分析

为了验证各关键模块的有效性，本文在统一实验设置下分别移除多尺度时间编码模块、时间感知动态图模块和图扩散注意力机制，构建三种模型变体，并在 METR-LA 和 PEMS-BAY 数据集上开展消融实验。结果如表 3 所示。

| 模型变体 | METR-LA MAE | METR-LA RMSE | PEMS-BAY MAE | PEMS-BAY RMSE |
|----------|-------------|--------------|--------------|---------------|
| w/o TCM | 2.89 | 5.06 | 1.85 | 2.96 |
| w/o TAG | 2.84 | 4.97 | 1.67 | 2.81 |
| w/o GDA | 2.78 | 4.86 | 1.44 | 2.63 |
| Full Model | 2.51 | 4.57 | 1.18 | 2.37 |

消融实验结果显示，完整模型在各项指标上均取得最优性能，说明三个关键模块在整体框架中都发挥了不可替代的作用。移除时间卷积模块后，模型性能下降最明显，尤其是在 PEMS-BAY 数据集上 MAE 从 1.18 上升到 1.85，说明多尺度时间建模对于长时间依赖提取和误差稳定控制具有关键作用。由于交通序列同时包含短期波动和周期模式，如果缺乏足够的时间感受野，模型在进行多步预测时容易出现趋势判断滞后和误差放大。

移除时间感知动态图模块后，模型在两个数据集上的误差均明显增加，说明仅依赖静态空间关系难以准确刻画交通网络中的时变关联结构。尤其在交通状态变化频繁的场景中，节点之间的影响强度并不是固定的，因此动态图生成机制对提升模型的时空适应性具有重要意义。进一步移除图扩散注意力机制后，模型性能继续下降，表明扩散传播和关键空间依赖建模对于空间特征提取同样十分关键。综合来看，TCM、TAG 和 GDA 三个模块分别从时间建模、动态图结构学习和空间传播增强三个层面共同构成了性能提升的核心来源。

为进一步分析模型中动态空间依赖建模模块的作用，本文对模型在三个数据集上学习到的动态依赖矩阵进行了可视化。热力图均呈现出明显的非均匀与稀疏分布特征，即大多数节点对的依赖权重较小，仅少数节点对具有显著较高的权重。这表明模型能够通过动态学习机制自适应地筛选对预测任务更为关键的空间关联，而非对所有空间连接赋予相同的重要性。不同数据集呈现不同的依赖模式：METR 以局部强关联为主，PEMS 的依赖分布相对更加分散，而 NE-BJ 则呈现出较为明显的带状结构，反映出其空间依赖中存在对多个节点产生共同影响的关键节点特征。

### 4.5 计算效率与工程代价分析

在追求预测精度的同时，交通预测模型还需兼顾训练与部署成本。表 4 给出了不同模型在 METR-LA 数据集上的训练耗时、推理耗时和显存占用对比结果。

| 模型 | 训练耗时（s/epoch） | 推理耗时（s） | GPU 显存占用（GB） |
|------|---------------------|---------------|-------------------|
| STGCN | 72.6 | 3.8 | 6.1 |
| Graph WaveNet | 95.4 | 4.9 | 8.7 |
| GMAN | 218.3 | 12.6 | 17.8 |
| DGCRN | 156.7 | 8.4 | 7.3 |
| ASTGCN | 131.5 | 6.9 | 9.2 |
| RGDAN | 105.2 | 5.6 | 10.5 |
| **ST-DGATNet** | **112.8** | **5.3** | **9.8** |

可以看出，ST-DGATNet 的训练耗时和显存占用均处于中等水平。与结构较为简单的 STGCN 相比，其计算代价有所增加，但明显低于 GMAN 这类全局注意力模型；与 RGDAN 和 DGCRN 相比，所提模型虽然引入了动态图和扩散注意力机制，但并未显著增加推理开销，反而在推理速度上略优于 RGDAN。这说明本文模型在提升预测性能的同时，仍然保持了较好的工程可部署性。

进一步从应用视角看，交通管理场景对模型的需求往往不是单纯追求极致精度，而是在精度、稳定性和资源开销之间取得平衡。ST-DGATNet 在中长期预测任务中表现出更高的准确率，同时训练和推理成本处于可控范围内，因此更适合作为智能交通平台中的预测核心模块使用。

---

## 5 结论

本文针对交通流量预测中动态空间依赖难以刻画和多尺度时间模式建模不足的问题，提出了一种基于时空扩散图注意力网络的交通流量预测方法。该方法通过多尺度时间编码模块提取长短期时间依赖，通过时间感知动态图模块实现随时间语义变化的空间关系学习，并利用图扩散注意力机制增强对有向传播过程和潜在全局依赖的建模能力。实验结果表明，所提方法在 METR-LA、PEMS-BAY 和 NE-BJ 三个公开数据集上的不同预测跨度下均取得了优于主流基线模型的性能，尤其在长期预测和复杂城市路网场景中表现突出。消融实验与效率分析进一步说明，模型设计兼顾了方法有效性与工程实用性。

本文工作表明，将时间语义驱动的动态图学习与多尺度时间编码、扩散传播机制进行协同融合，是提升交通流量预测性能的一条有效路径。后续研究可进一步引入天气、节假日、突发事件和区域功能属性等多源异构信息，以增强模型在跨城市迁移、实时更新和复杂场景决策支持中的适用能力。

---

## 参考文献

[1] 参考文献1（智慧城市与交通时空数据相关）

[2] 参考文献2（高维时空数据形态）

[3] 参考文献3（传统统计方法局限性）

[4] 参考文献4（深度学习在交通预测中的应用）

[5] 参考文献5（复杂动态系统视角）

[6] STGCN 原始论文

[7] DCRNN 原始论文（数据划分标准）

[8] Graph WaveNet 原始论文

[9] 参考文献9（统计方法 ARIMA 等）

[10] MTGNN 原始论文

[11] 参考文献11（中长期预测误差累积问题）

[12] 参考文献12（机器学习方法）

[13] RNN 在交通预测中的应用

[14] LSTM 原始论文

[15] GRU 原始论文

[16] TCN 原始论文

[17] Transformer 原始论文

[18] 参考文献18（时空依赖建模必要性）

[19] VAR 方法在时空预测中的应用

[20] 参考文献20（大规模城市场景限制）

[21] CNN 在时空预测中的应用

[22] GNN 在时空序列预测中的应用

[23] GCN 与 GAT 原始论文

[24] STGNN 综述

[25] 动态图方法

[26] 时空特征融合问题

[27] 蜂窝网络流量预测综述

[28] 早期蜂窝流量预测方法

[29] 机器学习在蜂窝流量中的应用

[30] 空间维度引入蜂窝流量预测

[31] STGNN 在蜂窝流量中的应用

[32] 交通流量预测综述

[33] 传统交通流理论模型

[34] RNN 在交通预测中的应用

[35] GNN 在交通预测中的应用

[36] 动态图在交通预测中的应用

[37] 多源信息融合在交通预测中的应用

[38] 人类活动特征在交通预测中的应用

[39] 时间序列概率论基础

[40] 多变量时间序列表示

[41] 三阶张量表示

[42] 时间序列乘法模型

[43] 严平稳性定义

[44] 弱平稳性定义

[45] MLP 理论基础

[46] MLP 逐层映射

[47] RNN 基础理论

[48] RNN 长距离依赖

[49] LSTM 细胞状态

[50] LSTM 训练成本

[51] GRU 结构

[52] 自注意力机制

[53] 多头注意力

[54] 状态空间模型

[55] SSM 卷积核结构

[56] 结构化状态空间模型

[57] 图的定义

[58] 图拉普拉斯矩阵

[59] 图信号定义

[60] 拉普拉斯特征分解

[61] 谱域图卷积

[62] 一阶近似 GCN

[63] 图注意力网络

[64] Softmax 归一化

[65] 多头图注意力

[66] 参考文献66

[67] 参考文献67

[68] 参考文献68

[69] 参考文献69

[70] 参考文献70

[71] 编码器解码器拼接方法

[72] 参考文献72

[73] 参考文献73

[74] 参考文献74

[75] 参考文献75

[76] 参考文献76

[77] 参考文献77

[78] 扩张因果卷积在长序列中的应用

[79] 动态图方法在交通预测中的应用

[80] 节点表征增强方法
