# 🔧 网页访问超时 - 故障排查与解决

## 📊 诊断结果

### ✅ 已确认正常
- OpenClaw 网关运行正常（PID: 594643）
- 端口 11676 正常监听（0.0.0.0，所有接口）
- 本地访问测试成功（HTTP 200）
- 无防火墙阻止

### ⚠️ 发现的问题
你的服务器有**两个 IP 地址**：
- `172.24.56.3`（主 IP）
- `10.255.0.1`（可能是 Docker 或虚拟网络）

浏览器可能在使用错误的网络路径。

---

## 🚀 解决方案（按顺序尝试）

### 方案 1：使用 localhost 访问（本机）

**最简单的方法！**

在**同一台服务器上**的浏览器访问：
```
http://localhost:11676/22131ecf/
```

或
```
http://127.0.0.1:11676/22131ecf/
```

---

### 方案 2：重启网关服务

有时网关服务需要重启以刷新网络绑定：

```bash
# 重启网关
openclaw gateway restart

# 等待 5 秒
sleep 5

# 检查状态
openclaw gateway status
```

然后再次访问：
```
http://172.24.56.3:11676/22131ecf/
```

---

### 方案 3：清除浏览器缓存

**Chrome/Edge：**
1. 按 `Ctrl+Shift+Delete`（Windows）或 `Cmd+Shift+Delete`（Mac）
2. 选择"缓存的图片和文件"
3. 点击"清除数据"
4. 刷新页面（F5 或 Ctrl+R）

**Firefox：**
1. 按 `Ctrl+Shift+Delete`
2. 选择"缓存"
3. 点击"立即清除"

**手机浏览器：**
- iOS Safari：设置 → Safari → 清除历史记录与网站数据
- Android Chrome：设置 → 隐私 → 清除浏览数据

---

### 方案 4：使用无痕模式

**Chrome/Edge：** `Ctrl+Shift+N`  
**Firefox：** `Ctrl+Shift+P`

在无痕窗口中访问：
```
http://172.24.56.3:11676/22131ecf/
```

---

### 方案 5：检查代理设置

如果你使用了代理：

**Chrome/Edge：**
1. 设置 → 系统 → 打开计算机的代理设置
2. 确保没有启用代理
3. 或在代理例外中添加：`172.24.56.3`

**Firefox：**
1. 选项 → 常规 → 网络设置
2. 选择"不使用代理"
3. 或在"不使用代理服务器"列表中添加 `172.24.56.3`

---

### 方案 6：尝试另一个 IP

你的服务器有两个 IP，可以尝试另一个：

```
http://10.255.0.1:11676/22131ecf/
```

---

### 方案 7：检查网络连通性

**在客户端电脑上执行：**

```bash
# Windows (命令提示符)
ping 172.24.56.3
telnet 172.24.56.3 11676

# Linux/Mac
ping 172.24.56.3
nc -zv 172.24.56.3 11676
```

如果 ping 不通，检查：
- 是否在同一局域网
- 路由器设置
- 网络隔离策略

---

## 🔍 快速诊断脚本

运行此脚本自动诊断：

```bash
#!/bin/bash
echo "=== OpenClaw 网页访问诊断 ==="
echo ""

# 1. 检查网关
echo "1. 网关状态:"
openclaw gateway status | grep -E "(Gateway|Runtime)"

# 2. 检查端口
echo ""
echo "2. 端口监听:"
netstat -tlnp 2>/dev/null | grep 11676 || echo "端口未监听"

# 3. 本地测试
echo ""
echo "3. 本地访问测试:"
curl -s -o /dev/null -w "HTTP %{http_code}\n" http://localhost:11676/22131ecf/

# 4. IP 地址
echo ""
echo "4. 服务器 IP:"
hostname -I

# 5. 建议
echo ""
echo "5. 建议访问地址:"
echo "   http://localhost:11676/22131ecf/"
```

---

## 💡 推荐方案

### 如果你在**服务器本机**使用浏览器：
✅ 使用：`http://localhost:11676/22131ecf/`

### 如果你在**其他电脑**访问：
1. 确保两台电脑在同一局域网
2. 先 ping 测试：`ping 172.24.56.3`
3. 使用：`http://172.24.56.3:11676/22131ecf/`

### 如果你在**手机**访问：
1. 确保手机连接同一 WiFi
2. 使用：`http://172.24.56.3:11676/22131ecf/`
3. 如果不行，尝试清除浏览器缓存

---

## 🆘 仍然无法访问？

### 收集以下信息：

1. **你在哪里访问？**
   - [ ] 服务器本机
   - [ ] 同一局域网的其他电脑
   - [ ] 手机

2. **使用什么浏览器？**
   - [ ] Chrome
   - [ ] Edge
   - [ ] Firefox
   - [ ] Safari
   - [ ] 其他：_______

3. **错误信息：**
   - [ ] 连接超时
   - [ ] 无法连接
   - [ ] 404 错误
   - [ ] 其他：_______

4. **ping 测试结果：**
   ```bash
   ping 172.24.56.3
   # 能 ping 通吗？
   ```

5. **端口测试结果：**
   ```bash
   telnet 172.24.56.3 11676
   # 或
   nc -zv 172.24.56.3 11676
   # 能连接吗？
   ```

提供这些信息，我可以更精准地帮你解决问题！

---

## ✅ 验证成功

访问成功后，你应该看到：
- OpenClaw 的聊天界面
- 左侧有历史对话列表
- 中间是聊天区域
- 可以输入消息

---

**最后更新：** 2026-04-18  
**当前状态：** 网关运行正常，等待访问验证
